#!/bin/bash
# Pre-commit hook: validate critical rules before commit.
# v1 (basePath/src/ query-name checks) was removed with the v1 codebase — no v2
# equivalent invariant defined yet; re-add checks here if one emerges.

echo "Pre-commit: All checks passed."
