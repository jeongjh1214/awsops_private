# UX Wave A Implementation Plan (C02·C01·C03·C06·C30)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** UX 리뷰(PR #45) 패널 만장일치 Wave A 구현 — 로그아웃/신원 신뢰버그(C02), 전역 refresh+staleness(C01), PageHeader 의무화+다크테마 잔재(C03), 홈 KPI 파생 서브라인(C06), 비용 서비스 드릴다운(C30). 전부 read-only.

**Architecture:** 신규 BFF 라우트 2개(`/api/me`, `/api/auth/signout`) + 비용 상세 라우트 1개(`/api/cost/detail`) + 기존 `RefreshButton`/`StatTile.hint` 재사용 + summary SQL 확장. Terraform은 web task env 2줄(`COGNITO_DOMAIN`, `APP_DOMAIN`)만.

**Tech Stack:** Next.js 14 BFF, vitest(+jsdom), node-pg JSONB, Cost Explorer SDK, Terraform env.

**근거 문서:** `docs/reviews/2026-06-12-v1-v2-ux-review.md` + 정찰 wf_af65ca64 (file:line 전부 검증됨)

**파일 스코프 (scope guard):**
- Create: `web/app/api/me/route.ts`, `web/app/api/me/route.test.ts`, `web/app/api/auth/signout/route.ts`, `web/app/api/auth/signout/route.test.ts`, `web/app/api/cost/detail/route.ts`, `web/app/api/cost/detail/route.test.ts`, `web/components/shell/UserIdentity.tsx`
- Modify: `web/components/shell/Sidebar.tsx`, `web/components/ui/PageHeader.tsx`, `web/app/page.tsx`, `web/app/cost/page.tsx`, `web/app/bedrock/page.tsx`, `web/app/topology/page.tsx`, `web/app/opencost/page.tsx`, `web/app/jobs/page.tsx`, `web/app/eks/page.tsx`, `web/app/ai-diagnosis/page.tsx`, `web/app/customization/page.tsx`, `web/app/api/inventory/summary/route.ts`, `web/lib/aws.ts`, `web/lib/aws.test.ts`, `web/lib/i18n.ts`, `terraform/v2/foundation/workload.tf`, `terraform/v2/foundation/edge-lambda/cognito_edge.py.tftpl`, `web/components/ui/RefreshButton.tsx`, `web/app/eks/eks-list.test.tsx`

---

### Task 1: C02 — 로그아웃 배선 + 실제 신원

**Files:**
- Create: `web/app/api/me/route.ts`, `web/app/api/me/route.test.ts`, `web/app/api/auth/signout/route.ts`, `web/app/api/auth/signout/route.test.ts`, `web/components/shell/UserIdentity.tsx`
- Modify: `web/components/shell/Sidebar.tsx`, `terraform/v2/foundation/workload.tf`

정찰 사실: 죽은 버튼 `Sidebar.tsx:153-159`(onClick 없음), 하드코딩 이메일 `:151`. 쿠키 `awsops_token`=HttpOnly id_token → 클라이언트 직접 읽기 불가, `verifyUser`(서버)가 `{sub,email,groups}` 반환. `COGNITO_CLIENT_ID`는 web env에 이미 존재, **`COGNITO_DOMAIN`은 미전달**. Cognito 클라이언트 `logout_urls=["https://${var.domain_name}/"]` 기구성. 엣지에 signout 경로 없음(불필요 — 앱이 쿠키 클리어+hosted-UI logout 리다이렉트로 충분; 복귀 시 엣지가 무토큰→로그인 유도).

- [ ] **Step 1 (TDD): `/api/me`** — `route.test.ts`: 401(미인증) / 200 `{email,groups,sub}`(verifyUser mock). 구현:

```ts
import { verifyUser } from '@/lib/auth';
export const dynamic = 'force-dynamic';
export async function GET(request: Request) {
  const user = await verifyUser(request.headers.get('cookie'));
  if (!user) return Response.json({ status: 'error', message: 'unauthenticated' }, { status: 401 });
  return Response.json({ sub: user.sub, email: user.email, groups: user.groups ?? [] });
}
```

- [ ] **Step 2 (TDD): `/api/auth/signout`** — POST: 인증 무관(만료 토큰으로도 로그아웃 가능해야 함). `awsops_token` Max-Age=0 클리어 + hosted-UI logout URL 반환:

```ts
export const dynamic = 'force-dynamic';
// HttpOnly 쿠키는 서버만 지울 수 있다. Cognito 세션도 hosted-UI /logout으로 끊는다
// (logout_uri는 auth.tf:30 logout_urls에 기등록된 https://{APP_DOMAIN}/).
export async function POST() {
  const domain = process.env.COGNITO_DOMAIN ?? '';
  const clientId = process.env.COGNITO_CLIENT_ID ?? '';
  const appDomain = process.env.APP_DOMAIN ?? '';
  const logoutUrl = domain && clientId && appDomain
    ? `https://${domain}/logout?client_id=${encodeURIComponent(clientId)}&logout_uri=${encodeURIComponent(`https://${appDomain}/`)}`
    : '/';
  return new Response(JSON.stringify({ logoutUrl }), {
    status: 200,
    headers: {
      'content-type': 'application/json',
      'set-cookie': 'awsops_token=; Path=/; Secure; HttpOnly; SameSite=Lax; Max-Age=0',
    },
  });
}
```
테스트: set-cookie에 `Max-Age=0` 포함 / env 3종 세팅 시 logoutUrl 형식 / env 부재 시 `'/'` 폴백.

- [ ] **Step 3: `UserIdentity.tsx`** (client) — `/api/me` fetch → 이메일·그룹 렌더 + 로그아웃 버튼(onClick: `POST /api/auth/signout` → `window.location.href = logoutUrl`). 로딩/401 시 기존 플레이스홀더 형태 유지. Sidebar.tsx:148-160 블록을 `<UserIdentity />`로 교체(스타일 클래스 보존, `LogOut` 아이콘·`t('sidebar.signOut')` aria-label 유지).
- [ ] **Step 3b (P2: codex MAJOR)**: `terraform/v2/foundation/edge-lambda/cognito_edge.py.tftpl`의 `is_public` 목록에 `'/api/auth/signout'` 추가 — 엣지는 현재 `/_next/static/*`·`/api/health`만 통과시키므로 **만료 토큰 사용자가 로그아웃을 못 하는**(엣지가 Cognito로 302 → 새 토큰 무한 재발급) 함정. signout은 쿠키 클리어+리다이렉트 URL 반환뿐이라 공개해도 안전(CSRF로 강제돼도 결과는 '로그아웃' — 상태 파괴 없음, gemini 동의). ⚠️ 엣지 Lambda 변경 = us-east-1 배포+CloudFront 전파 — **apply는 컨트롤러가 별도 실행**(이 PR은 코드만).

- [ ] **Step 4: workload.tf** — web task env에 추가(in-place, 기존 203-204행 옆):

```hcl
{ name = "COGNITO_DOMAIN", value = "${aws_cognito_user_pool_domain.main.domain}.auth.${var.region}.amazoncognito.com" },
{ name = "APP_DOMAIN", value = var.domain_name },
```
`terraform validate` Success 확인.
- [ ] **Step 5**: vitest 신규+전체 green → 커밋 `feat(ux-a): C02 — wire sign-out (cookie clear + hosted-UI logout) and real identity from JWT`

---

### Task 2: C01 — 전역 refresh + staleness 스탬프

**Files:**
- Modify: `web/components/ui/PageHeader.tsx`, `web/app/page.tsx`, `web/app/cost/page.tsx`, `web/app/bedrock/page.tsx`, `web/app/topology/page.tsx`, `web/app/opencost/page.tsx`

정찰 사실: `RefreshButton`(busy/onClick/capturedAt, 30분 임계 '오래됨') 기존재 — 소비자는 inventory 1곳. PageHeader `right` 슬롯 존재, `live` 배지는 장식. home/cost/bedrock/topology는 fetch가 inline useEffect라 추출 필요.

- [ ] **Step 0 (P2: gemini)**: `RefreshButton.tsx`의 래퍼 `mb-3` 제거 — PageHeader right 슬롯의 수직 중앙 정렬을 깨뜨림(유일 기존 소비자 inventory도 무해).
- [ ] **Step 1: 페이지별 fetch 추출 + 배선** — 각 페이지에서 inline fetch를 `const load = useCallback(...)`로 추출하고 성공 시 `setCapturedAt(new Date().toISOString())`. `PageHeader right={<RefreshButton busy={busy} onClick={load} capturedAt={capturedAt} />}` 연결:
  - **home** `app/page.tsx`: 3-fetch를 `loadAll`로 (각 fetch 독립 degrade 유지 — Promise.allSettled), **`live` prop 제거**(장식 배지 퇴출 — C01의 핵심 서사)
  - **cost**: `/api/cost` 추출
  - **bedrock**: `load(range)` 형태 — range 변경 useEffect는 유지하고 버튼은 현재 range 재호출
  - **topology**: `Promise.all(TYPES.map(fetchType))` 추출
  - **opencost**: 기존 fetch 추출(파일 확인 후 동일 패턴)
- [ ] **Step 2: PageHeader `live` 배지 의미 수정** — prop은 유지하되(타 페이지 호환) JSDoc에 "정말 스트리밍/폴링하는 표면에만 사용"을 명시. home에서 제거된 것 확인.
- [ ] **Step 3**: 기존 테스트 무회귀(eks-list 등) + 커밋 `feat(ux-a): C01 — real refresh + staleness stamp on home/cost/bedrock/topology/opencost (decorative 'live' badge retired)`

---

### Task 3: C03 — PageHeader 의무화 + 다크테마 잔재 제거

**Files:**
- Modify: `web/app/jobs/page.tsx`, `web/app/eks/page.tsx`, `web/app/ai-diagnosis/page.tsx`, `web/app/customization/page.tsx`, `web/app/eks/eks-list.test.tsx`

- [ ] **Step 1: jobs** — `<h1 style={{color:'#e6eefb',...}}>`(:13)와 `#ef4444`/`#7da2c9` 인라인 다크 색·`<main style>` 전부 제거 → `PageHeader title="Async Jobs"` + paper/ink Tailwind(`text-rose-600`, `text-ink-400`)로 교체. jobs에도 RefreshButton 배선(fetch 추출).
- [ ] **Step 2: eks** — `:171` hand-rolled h1 → `PageHeader title="EKS Clusters" subtitle={기존 문구} right={<RefreshButton busy={...} onClick={() => { load(); loadFleet(); }} capturedAt={...} />}` (`load`/`loadFleet`은 이미 useCallback — 정찰 확인). `eks-list.test.tsx`가 h1 텍스트에 의존하면 갱신.
- [ ] **Step 3: ai-diagnosis / customization** — 동일 치환(`PageHeader` 적용, 기존 subtitle 보존).
- [ ] **Step 4**: vitest 전체 green + 커밋 `feat(ux-a): C03 — PageHeader everywhere; /jobs dark-theme leftovers removed`

---

### Task 4: C06 — 홈 KPI 파생 서브라인

**Files:**
- Modify: `web/app/api/inventory/summary/route.ts`, `web/app/page.tsx`

정찰 사실: summary는 type별 count만. `inventory_resources.data`(JSONB)에 ec2 `instance_state`·ebs `encrypted`·iam_user `mfa_enabled` 동기화됨; SG는 `ip_permissions` 원본 JSON(플래그 없음). s3 public은 미동기(제외 — C16). `StatTile.hint?: ReactNode` 기존재. 홈 jobs 타일만 hint 사용 중.

- [ ] **Step 1 (TDD): summary `splits` 확장** — 라우트 테스트(pool.query mock)로 신규 필드 고정:

```ts
// 추가 쿼리 (단일 왕복, UNION ALL):
const SPLITS_SQL = `
  SELECT 'ec2_running' AS k, count(*)::int AS n FROM inventory_resources WHERE account_id='self' AND resource_type='ec2' AND data->>'instance_state'='running'
  UNION ALL SELECT 'ec2_stopped', count(*)::int FROM inventory_resources WHERE account_id='self' AND resource_type='ec2' AND data->>'instance_state'='stopped'
  UNION ALL SELECT 'ebs_unencrypted', count(*)::int FROM inventory_resources WHERE account_id='self' AND resource_type='ebs_volume' AND (data->>'encrypted')='false'
  UNION ALL SELECT 'iam_user_no_mfa', count(*)::int FROM inventory_resources WHERE account_id='self' AND resource_type='iam_user' AND (data->>'mfa_enabled')='false'
  -- SG 인그레스 개방: ip_permissions(인그레스 전용 키 — egress는 별도 키, gemini 검증)에서
  -- cidr 필드에 앵커된 정규식(설명 텍스트 오탐 차단, codex) + IPv6 ::/0 포함. 키 케이싱은
  -- Steampipe 원본 보존 가능성 양쪽(cidr_ip/CidrIp) 매칭.
  UNION ALL SELECT 'sg_open_ingress', count(*)::int FROM inventory_resources
    WHERE account_id='self' AND resource_type='security_group'
    AND (data->'ip_permissions')::text ~ '"(cidr_ip|CidrIp|cidr_ipv6|CidrIpv6)"\s*:\s*"(0\.0\.0\.0/0|::/0)"'
`;
// 응답: { byType, byCategory, total, splits: { ec2Running, ec2Stopped, ebsUnencrypted, iamUserNoMfa, sgOpenIngress } }
```
- [ ] **Step 2: 홈 타일 hint 배선** — `page.tsx`:
  - EC2 타일: `hint={\`\${s.ec2Running} running · \${s.ec2Stopped} stopped\`}`
  - EBS 타일: `hint={s.ebsUnencrypted > 0 ? \`미암호화 \${s.ebsUnencrypted}\` : '전체 암호화'}` + `variant={s.ebsUnencrypted > 0 ? 'warn' : 'default'}`
  - 보안 그룹 타일: `hint={\`인그레스 개방 \${s.sgOpenIngress}\`}` + `variant={s.sgOpenIngress > 0 ? 'warn' : 'default'}`
  - splits 부재(이전 응답 호환) 시 hint 생략 — optional chaining.
- [ ] **Step 3**: vitest green + 커밋 `feat(ux-a): C06 — derived KPI sublines (EC2 run/stop, EBS unencrypted, SG open-ingress) via summary splits`

---

### Task 5: C30 — 비용 서비스 드릴다운

**Files:**
- Modify: `web/lib/aws.ts`, `web/lib/aws.test.ts`, `web/app/cost/page.tsx`
- Create: `web/app/api/cost/detail/route.ts`, `web/app/api/cost/detail/route.test.ts`

정찰 사실: `/api/cost`의 monthly는 서비스별 분해를 합산-폐기. **동시 세션의 `getServiceCostDetail` 소스는 유실**(.next 산출물만) — 동일 시그니처로 신규 구현(향후 충돌 최소화): `{service, currency, trend|null, byUsageType|null}` (null=해당 CE 호출 실패, []=정말 빈 것).

- [ ] **Step 1 (TDD): `rollupUsageTypes` 순수 함수** — `aws.test.ts`: 내림차순 정렬, top-n 유지+잔여 '기타' 합산, n 이하면 그대로, 잔여 0이면 '기타' 생략. 구현(`aws.ts`):

```ts
export interface ServiceUsageType { usageType: string; amount: number }
export interface ServiceCostDetail {
  service: string; currency: string;
  trend: CostTrendPoint[] | null;          // null = trend CE 호출 실패 (vs [] = 진짜 빈 것)
  byUsageType: ServiceUsageType[] | null;
}
export function rollupUsageTypes(rows: ServiceUsageType[], n: number): ServiceUsageType[] {
  const sorted = [...rows].sort((a, b) => b.amount - a.amount);
  if (sorted.length <= n) return sorted;
  const head = sorted.slice(0, n);
  const rest = sorted.slice(n).reduce((s, x) => s + x.amount, 0);
  return rest > 0 ? [...head, { usageType: '기타', amount: rest }] : head;
}
```

- [ ] **Step 2: `getServiceCostDetail(service)`** — 기존 `ce` 클라이언트 재사용: ① DAILY 30일 `Filter: {Dimensions:{Key:'SERVICE', Values:[service]}}` → trend; ② MONTHLY 3개월 `GroupBy USAGE_TYPE` + 동일 Filter → 합산 후 `rollupUsageTypes(rows, 8)`. 각각 `.catch(() => null)` (null 의미론 유지).
- [ ] **Step 3 (TDD): `/api/cost/detail?service=`** — 테스트: 401 / 400(service 누락·100자 초과) / 200 정상(모킹) / CE 실패 시 `{trend:null}` 전달. verifyUser 게이트 + `getServiceCostDetail` 호출.
- [ ] **Step 4: cost 페이지 드릴다운 패널** — `DataTable`(:140)에 `onRowClick` → 우측 패널(기존 DetailPanel 스타일 차용한 인라인 `<aside>` 또는 Card 영역): 서비스명 + `AreaTrend`(일별 30d) + `HBarList`(usage-type top-8+기타) + 로딩/실패("CE 호출 실패 — 잠시 후 재시도") 상태. Esc/×로 닫기. fetch는 클릭 시 lazy — **서비스명은 `encodeURIComponent`**(공백/구두점 포함, P2: codex).
- [ ] **Step 5**: vitest 전체 green + 커밋 `feat(ux-a): C30 — cost service drill-down (daily trend + usage-type rollup panel)`

---

### Task 6: 최종 검증

- [ ] `cd web && npx vitest run`(전체 green, 신규 ≥12) && `npx tsc --noEmit`(터치 파일 clean) && `npm run build`
- [ ] `terraform -chdir=terraform/v2/foundation validate` Success (workload.tf env 추가분)
- [ ] P4 누적 게이트(패널) → push → PR
