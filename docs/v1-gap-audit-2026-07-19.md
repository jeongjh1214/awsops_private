# v1 ↔ v2 메뉴 기능 격차 감사 (2026-07-19)

60-에이전트 병렬 감사(메뉴별 비교 → 적대 검증)로 확정된 242건. 이 문서 생성 시점에 1차 배치(스펙 기반 KPI/차트/대시보드 신호)가 구현·배포됨 — [x] 표시.

## P1 (39건)

- [x] **ai-diagnosis** [M] 계정(account) 스코프 진단 + 히스토리 Account 컬럼 — v1은 useAccountContext의 currentAccountId를 POST에 전달해 선택한 계정만 진단하고, 리포트 이력 테이블에 계정 alias 컬럼을 표시한다. v2 POST /api/diagnosis/route.ts:43은 env AWS_ACCOUNT_ID 단일 계정으로 고정이며 목록에도 계정 표시가 없다. Aurora sync 테이블은 account_id 컬럼을 이미 보유하므로
- [x] **cloudfront** [S] HTTPS 보안 KPI: 'HTTP Allowed' 카운트 + 권고 문구 — v1은 default_cache_behavior->>'ViewerProtocolPolicy' = 'allow-all'인 배포 수를 4번째 StatsCard로 표시하고, >0이면 orange + 'Consider HTTPS only', 0이면 green + '✓ All HTTPS' 권고를 보여줌. v2 cloudfront에는 HIGHLIGHTS 엔트리가 없어 stateKey('status') 
- [x] **cloudtrail** [M] Recent Events tab (CloudTrail lookup events) — v1 has a 'Recent Events' tab listing the last 20 CloudTrail lookup events with Time / Event / Source / User / Resource Type / Resource / Read-Write columns (lazy-loaded on tab click, count in tab label). v2 has no CloudT
- [x] **cloudwatch** [S] state_reason column in the alarm table — v1's table has a Reason column showing state_reason truncated to 80 chars with full text in a title tooltip — the fastest way to see WHY an alarm fired without opening the detail. v2's columns are state_value, metric_nam
- [x] **compliance** [S] 섹션 카드 drill-down (상태별 카운트 + 스택 진행바 + 섹션별 컨트롤 필터) — v1은 최상위 섹션마다 카드(OK/ALARM/SKIP 카운트, pass% 배지, ok/alarm/skip/error 4색 스택 진행바)를 렌더링하고, 카드 클릭 시 해당 섹션의 컨트롤만 재귀 추출(extractControls)해 확장 목록으로 표시. v2는 섹션별 pass% Meter(단색)뿐이고 Controls 테이블은 전체 결과 flat 목록이라 섹션 클릭→해당 섹션 컨트롤만 보기가 불가
- [x] **container-cost** [S] High-value task columns: CPU (units+vCPU), Memory (MB+GB), AZ, started_at, short Task ID — v1's task table shows cpu with vCPU conversion '512 (0.50 vCPU)', memory with GB conversion, availability_zone, and a 12-char mono task_id; the query also selects started_at. v2's ecs_task columns are only Group/Status/L
- [x] **cost** [S] 서비스 드릴다운의 계정 스코핑 누락 (항상 host 계정 데이터) — v1은 서비스 상세 조회 시 accountId: currentAccountId를 전달해 선택된 계정으로 스코핑. v2의 /api/cost/detail은 account 파라미터를 받지 않고 getServiceCostDetail이 ceClient()(host 자격)만 사용 — 멤버 계정이나 '전체 계정' 선택 상태에서 행을 클릭하면 조용히 host 계정의 수치가 표시됨(목록과 상세 불일치). d
- [x] **dashboard** [S] CloudWatch 알람 ALARM 상태 강조 + 활성 경고 행 — v1은 알람 수>0이면 타일을 red로 강조하고 활성 경고 목록에 'CloudWatch 알람 N건' 행(→/cloudwatch)을 추가. v2는 cloudwatch_alarm 리소스 총수만 중립 표시하고 경고 목록(s3/sg/ebs/mfa/jobs/k8s)에 알람 항목이 없음. v2 sync 테이블에 state_value가 이미 있고(inventory-types.ts:311 stateKey:
- [x] **dashboard** [M] 대시보드 계정/스코프 미적용 (host 계정 고정) — v1 대시보드는 useAccountContext의 currentAccountId를 모든 쿼리에 전달해 계정 전환·__all__ 통합 뷰를 지원. v2 대시보드는 AccountSelector/ScopeSelector(shell)와 account-context.ts가 존재함에도 page.tsx가 이를 전혀 사용하지 않고 /api/overview·/api/inventory/summary를 파라미터
- [x] **datasources** [S] Endpoint URL visibility (table column + edit form) — v1 lists the endpoint URL as a table column (mono, truncated, hover tooltip) and pre-fills it when editing. v2's table has no URL column and the Edit form opens with endpoint:'' (DatasourcesTab passes endpoint: ''), so t
- [x] **datasources** [M] `chart` Multi-series chart + Line/Bar toggle for Prometheus range results — v1 Explore renders Prometheus matrix results as up to 8 concurrent series with a per-series color palette, legend, custom tooltip, and a Line/Bar chart-type toggle. v2 charts ONLY the first series via AreaTrend (datasour
- [x] **dynamodb** [S] 가독 포맷팅: 크기(bytes)·아이템 수·과금 모드·생성일 — v1 테이블/상세는 table_size_bytes를 formatBytes(예: 1.2 GB)로, item_count를 toLocaleString으로, billing_mode를 'On-Demand'/'Provisioned' 친화 라벨로, creation_date_time을 toLocaleString으로 렌더. v2는 컬럼이 key/label만 갖고 renderCell이 String() 처리라 
- [x] **ebs** [M] 볼륨 테이블 'Attached To' 컬럼 — v1 볼륨 테이블은 attachments JSON을 파싱해 InstanceId(모노 cyan)+Device를 행마다 표시하고 미연결이면 'Unattached'를 표기 — 목록에서 바로 연결 상태를 스캔 가능. v2 ebs_volume 컬럼에 attachments가 없고(상세 패널에서만 idlist로 표시), v2 DataTable renderCell은 객체를 String()으로 뭉개므로 스칼
- [x] **ebs** [S] 유휴 볼륨 KPI (available 수 + 낭비 GB) — v1은 'Idle Volumes' KPI로 available 상태 볼륨 수와 'N GB wasted'(available_size_gb 합계)를 강조 표시하고, 총 용량 카드에도 'GB used · GB idle' 분해를 표기. v2는 총 용량 sum만 있고 유휴 볼륨 수/낭비 용량 KPI가 없음. countWhere(state=available) 하이라이트 추가 + 조건부 합계용 'sumWh
- [x] **ec2** [M] 멀티 어카운트: Account 컬럼 + 계정 스코프 데이터 — v1은 useAccountContext로 계정별 쿼리 스코핑(buildSearchPath), DataTable이 isMultiAccount && account_id 감지 시 AccountBadge 포함 Account 컬럼을 자동 추가하고 상세 패널에도 Account 행 표시. v2는 readResources/metrics 라우트가 account_id='self'로 하드코딩, 페이지 주석에도 
- [x] **ecs** [M] 멀티 어카운트: Account 컬럼 + 계정 스코프 조회 — v1은 useAccountContext로 선택 계정별 Steampipe 스코프 쿼리를 실행하고, DataTable이 멀티 어카운트 감지 시 Account 컬럼을 자동 추가하며 상세 패널에도 Account 행을 표시. v2는 sync_lambda가 account_id='self' 행만 적재하고 inventory 라우트가 accounts 파라미터를 무시(코드 주석: 'account filteri
- [x] **eks-container-cost** [M] OpenCost allocation API 비용 데이터 조회 (백엔드) — v1은 opencostEndpoint의 /allocation/compute?window=1d&aggregate=namespace,pod를 호출해 Pod별 CPU/Memory/Network/Storage(PV)/GPU 5개 비용 항목을 24h 환산·집계한다. v2는 OpenCost를 '설치'만 시키고 (status/bundle 라우트) 설치된 OpenCost에서 데이터를 읽는 라우트가 전혀 없
- [x] **eks-container-cost** [M] Pod별 비용 테이블 (OpenCost 모드에서 Network/Storage/GPU 컬럼 동적 추가) — namespace/pod/node/CPU비용/Memory비용/Total/Day 컬럼의 정렬된 테이블. dataSource==='opencost'일 때만 Network·Storage·GPU 비용 컬럼이 동적으로 추가된다. Pod당 컨테이너별 요청 병합 집계 포함. v2 pods 탭에는 비용 컬럼이 전혀 없다.
- [x] **eks-container-cost** [M] 비용 KPI 카드 4종 — Pod Cost (Daily), Pod Cost (Monthly, ×30 프로젝션), Running Pods/Nodes 카운트, Top Namespace(이름+일비용). 필터 적용 시 클라이언트에서 재계산. v2 클러스터 상세에는 비용 KPI가 전혀 없다.
- [x] **elasticache** [M] 노드 테이블 라이브 CloudWatch 메트릭 (CPU/EngineCPU/Memory/NetIn/NetOut/Connections 인라인 바) — v1은 GET /api/elasticache?clusterIds=…로 CloudWatch get-metric-data(AWS/ElastiCache, 6개 메트릭, 최근 1시간)를 일괄 조회해 노드 테이블에 CPU/Engine CPU 프로그레스 바(임계값 색상), Freeable Memory GB, Network In/Out MB, Connections를 인라인 표시. v2의 /api/inve
- [x] **iam** [S] Admin-only access gating on IAM menus — v1 gates the IAM page behind an admin check (GET /awsops/api/steampipe?action=admin-check) and renders a dedicated 'Access Denied' screen for non-admins. In v2, /inventory/iam_user and /inventory/iam_role and their BFF r
- [x] **inventory-home** [M] `chart` 리소스 타입별 멀티라인 추이 차트 — v1은 ~27개 리소스 타입(EC2/RDS/S3/EBS/Lambda/VPC/IAM/K8s/보안 파생 등)을 타입별 고유 색상 라인으로 한 차트에 겹쳐 그리고, 값 내림차순 정렬된 커스텀 툴팁을 제공. v2는 total+EC2 단일 area+line(AreaTrend, 14d 고정)뿐이고 AreaTrend는 최대 2시리즈만 지원 — 임의 N시리즈 라인 차트 컴포넌트 자체가 없음. Aurora 
- [x] **inventory-home** [M] 추이 델타 테이블 (Current / 7d Ago / 30d Ago / 7d·30d Change %) — v1은 전체 리소스 타입을 현재수량 내림차순으로 나열하고 7일/30일 전 스냅샷(±2일 허용 근사 매칭) 대비 절대·퍼센트 변화를 증가=orange/감소=cyan 색상으로 표시하는 테이블 제공. v2에는 어떤 형태의 수량 변화 이력 테이블도 없음(현재값 타일뿐). per-type trend API만 있으면 클라이언트 계산으로 구현 가능.
- [x] **k8s-eks** [M] Multi-account / multi-region cluster listing — v1's eksClusterList runs on the Steampipe aggregator: clusters from every configured account and region, with account_id and region columns, scoped by the account picker (useAccountContext → accountId per fetch); the sid
- [x] **k8s-eks** [M] Node ENI panel (network interfaces + IP capacity) — v1 node detail shows the node's ENIs (queried from aws_ec2_network_interface via private_dns_name match): ENI id, status badge, interface type, primary IP, allocated IP count with secondary-IP breakdown, an 'IP Slots Use
- [x] **lambda** [M] Multi-account: Account column + account-scoped data — v1 fetches with accountId (useAccountContext → buildSearchPath) and DataTable auto-adds an Account column when isMultiAccount && rows have account_id; the detail panel shows an Account row. v2's inventory route explicitl
- [x] **monitoring** [M] `chart` EC2 인스턴스 상세 멀티 메트릭 시계열 차트 + 기간 선택기 — v1은 EC2 행 클릭 시 CPUUtilization, NetworkIn/Out, DiskRead/WriteOps, NetworkPacketsIn/Out 7종 CloudWatch 시계열 라인 차트를 2컬럼 그리드로 렌더 (각 차트에 Avg/Max 최신값, 메트릭별 색상), 1h/6h/24h/7d/30d 기간 선택기(period 300/3600/86400 자동 전환), 'CloudWatch d
- [x] **monitoring** [S] EC2/RDS 테이블의 인스턴스별 라이브 메트릭 컬럼 (임계값 색상) — v1은 EC2 탭 테이블에 Avg/Max CPU% 컬럼, RDS 탭 테이블에 Avg/Max CPU% + Avg/Max Connections 컬럼을 >80% 빨강 / >50% 주황 / 그 외 초록 임계값 색상으로 표시. v2 인벤토리 테이블은 동기화된 설정 컬럼만 있고 메트릭 컬럼이 없음 (RDS는 상세 패널에서만, EC2는 fleet 평균만). RDS는 rdsMetrics()의 byInsta
- [x] **msk** [S] Bootstrap Broker 접속 문자열 (Plaintext/TLS) — v1 상세 패널은 bootstrap_broker_string과 bootstrap_broker_string_tls를 복사 가능한 형태로 표시 — 운영자가 클라이언트 접속 설정 시 바로 쓰는 값. v2 sync_lambda의 msk SELECT에 이 컬럼들이 없어 데이터 자체가 Aurora에 미존재. sync 쿼리에 2개 컬럼 추가 + spec에 Connectivity 섹션 추가로 해결.
- [x] **msk** [S] 고가치 테이블 컬럼: Kafka 버전/브로커 수/인스턴스 타입/EBS/모니터링/생성일 — v1 목록 테이블 컬럼: kafka_version, instance_type, number_of_broker_nodes, ebs_volume_gb, enhanced_monitoring(DEFAULT 여부 색상), creation_time. v2 테이블은 ID, Region, state, cluster_type, current_version(내부 버전 문자열이지 Kafka 버전이 아님)만 노출
- [x] **opensearch** [M] 라이브 CloudWatch 도메인 메트릭 테이블 (12개 메트릭) — v1은 도메인별 CloudWatch(AWS/ES) 최근 1시간 메트릭 테이블을 렌더링: Cluster Status GREEN/YELLOW/RED 필, CPU % 및 JVM Memory Pressure(임계값 색상 프로그레스 바), Nodes, Searchable Documents, Free Storage GB, Search/Index Rate, Search/Index Latency. v2의 
- [x] **rds** [M] Fleet 인스턴스별 CloudWatch 메트릭 테이블 — v1은 목록 아래에 전체 인스턴스 × 8개 live 메트릭(CPU/Free Memory/Connections/Read·Write IOPS/Net In·Out/Free Storage)을 한눈에 보여주는 별도 테이블을 렌더링 (CPU는 80%/50% 임계값 색상 프로그레스바, 행 클릭 시 상세 열림, 'last 1h' 표기). v2는 fleet 메트릭을 평균 CPU·총 커넥션·최소 여유 스토리지
- [x] **rds** [M] 멀티 어카운트 스코핑 (Account 컬럼 + 계정별 메트릭) — v1은 isMultiAccount 시 목록·메트릭 테이블·상세에 Account 컬럼/AccountBadge를 자동 표시하고, steampipe 쿼리와 /api/rds 메트릭 모두 accountId로 스코핑(계정별 profile/region). v2 rds는 inventory 쿼리·metrics route 모두 account_id='self' 하드코딩이며 페이지 주석에도 'account fil
- [x] **security** [S] Per-check high-value table columns (v2 collapses all checks to 3 generic columns) — v1 shows check-specific columns: Public S3 → Policy Public / Block ACLs / Block Policy as red-green flags; Open SGs → Group Name, VPC, Protocol, From Port, To Port, CIDR (0.0.0.0/0 highlighted red); Unencrypted EBS → Nam
- [x] **security** [L] Trivy CVE vulnerabilities check (5th tab) — table, KPI tiles, detail panel — v1 has an entire CVE tab backed by trivy_scan_vulnerability (top 200, severity-ordered): table with CVE ID, severity pill, package, installed/fixed version, title; two KPI StatsCards (CVE Critical, CVE High); and a detai
- [x] **topology** [M] NAT / IGW / TGW 리소스 가시성 — v1은 NAT Gateway(state 포함), Internet Gateway, Transit Gateway(VPC attachment)를 1급 노드/컬럼으로 표시. v2는 이 3개 리소스 타입이 inventory-types.ts와 sync_lambda.py에 아예 없어 flow/infra 어느 그래프에도 등장 불가. 동기화 쿼리 3개 + inventory 타입 spec + infra-top
- [x] **topology** [M] `chart` 계정 전체 인프라 배치 그래프 (Infra Graph View) — v1은 모든 VPC→Subnet→EC2, VPC→ELB/RDS, Subnet→NAT, IGW→VPC, TGW→VPC attachment 엣지를 한 ReactFlow 캔버스에 그리는 계정 단위 네트워크 배치 그래프 제공(타입별 색상 MiniMap 포함). v2는 요청 흐름 그래프뿐이고 배치 관계는 resource/[id]의 단일 리소스 ego-graph(depth≤3)로만 접근 가능 — 계정 
- [x] **vpc** [M] Route Table 목록/상세 (associations + routes 테이블) — v1은 Route Table 탭에서 route_table_id/name/vpc/association_count/route_count 목록을 보여주고, 행 클릭 시 Associations(서브넷/Main 여부)와 Routes(Destination/Target/State, blackhole은 흐리게+빨간색) 테이블을 렌더링. v2에는 route_table 인벤토리 타입이 없고 sync_lambd
- [x] **vpc** [S] Security Group 규칙 구조화 렌더링 (인바운드/아웃바운드) — v1은 SG 상세에서 ip_permissions/ip_permissions_egress를 파싱해 규칙별 카드(프로토콜 'All Traffic'/TCP 등 색상, 포트 범위, CIDR+설명, SG 참조는 보라색)로 렌더링. v2는 스펙에 'Ingress Rules'/'Egress Rules' 섹션 키는 있으나 inventory-detail.ts의 structuredList에 ip_permiss

## P2 (124건)

- [ ] **ai-diagnosis** [M] TOC 사이드바 + 섹션별 접기/펼치기 + 심각도 아이콘 — v1 완료 리포트는 15개 섹션을 개별 카드로 렌더링하며: sticky 목차 사이드바(클릭 시 smooth scroll, 토글 버튼), 섹션별 collapse/expand, 콘텐츠 내 critical/warning 키워드 수 기반 심각도 아이콘(XCircle/AlertTriangle/CheckCircle)을 제공한다. v2는 전체 마크다운을 단일 연속 문서로 렌더링해 15섹션 딥 리포트 내 
- [ ] **ai-diagnosis** [M] 리포트 생성 언어 선택 (ko/en/zh) — v1은 진단 실행 시 현재 UI 언어를 POST body의 lang으로 전달해 리포트 본문을 한국어/영어/중국어로 생성하고, 자동 스케줄에도 언어 선택 드롭다운이 있다. v2는 worker의 sections.py 프롬프트에 '한국어로 답하라'가 하드코딩되어 한국어 리포트만 생성 가능하며 POST /api/diagnosis에 lang 파라미터가 없다.
- [ ] **ai-diagnosis** [M] 스케줄 상세 설정: 요일/날짜/시각(KST) 선택 + 최근 실행 표시 — v1 스케줄 패널은 주기 외에 요일(weekly/biweekly), 날짜 1-28일(monthly), 실행 시각 00-23시(KST)를 선택할 수 있고 다음 실행과 최근 실행(lastRunAt)을 모두 표시한다. v2 SchedulePanel은 주기(매주/격주/매월) + on/off + tier/model만 지원하며 lastRunAt은 API가 반환하지만 UI에 표시하지 않는다. report
- [ ] **ai-diagnosis** [M] 진단 페이지 UI 다국어 (en/zh) — v1 페이지는 모든 라벨/버튼/상태 문구가 ko/en/zh 3개 언어(L() 헬퍼 + SECTION_FULL_NAMES/SUBTOPICS 트라이링구얼 맵)로 제공된다. v2 DiagnosisView/SchedulePanel/SubscribersPanel은 한국어 하드코딩이며, v2의 lib/i18n.ts는 shell/nav 전용(ko/en)이고 zh 자체가 없다. 기존 i18n 코어에 페이지
- [ ] **ai-diagnosis** [S] 테스트 알림 발송 버튼 — v1 알림 패널에는 등록된 이메일로 SNS 테스트 메시지를 즉시 발송하는 'Test' 버튼이 있어(POST /api/notification action:test) 구독 확인 후 수신 여부를 검증할 수 있다. v2 SubscribersPanel에는 구독 추가/제거만 있고 테스트 발송이 없다. BFF에서 SNS Publish 1건이면 구현 가능.
- [x] **bedrock** [S] Account Total vs AWSops App 사용량 비교 패널 (앱 토큰 사용량·모델별 호출) — v1은 CloudWatch 계정 전체(호출/입출력 토큰/비용)와 AWSops 앱 누적 사용량(AI Queries 수, 앱 입출력 토큰, 모델별 호출 수 tokensByModel)을 좌우 비교 패널로 표시. v2의 ChatOpsStatsCard는 호출수/성공률/평균응답/게이트웨이 분포만 보여주고 앱 토큰 사용량·모델별 호출·계정 전체와의 나란한 비교가 없음. v2 Aurora agentcore
- [x] **bedrock** [S] Avg Latency KPI 타일 — v1은 모델별 평균 지연을 집계해 페이지 상단 KPI(StatsCard, 초 단위)로 표시. v2는 지연을 테이블 컬럼으로만 노출하고 상단 StatTile 행(5개: 비용/호출/입력/출력/캐시절감)에 없음. v2 API가 avgLatencyMs를 이미 반환하므로 클라이언트 집계만 추가하면 됨.
- [x] **bedrock** [S] Errors KPI 타일 (조건부 색상) — v1은 4xx+5xx 합계를 KPI 타일로 표시하고 에러>0이면 red, 0이면 green으로 조건부 강조. v2는 에러가 모델 상세 테이블 컬럼에만 있고 페이지 수준 KPI가 없어 한눈에 에러 발생 여부를 알 수 없음.
- [x] **bedrock** [S] Prompt Caching 요약 패널 + 캐시 적중률 — v1은 캐시 활동이 있으면 전용 패널로 Cache Read Tokens / Cache Write Tokens / Hit Rate(%) / Cache Cost / Savings 5개 지표를 표시하고, 캐싱 KPI 타일에 'X% hit rate' 배지를 붙임. v2는 '캐시 절감' 단일 타일만 있고 캐시 read/write 토큰량·적중률·캐시 비용이 어디에도 없음. v2 API는 모델별 cach
- [x] **bedrock** [M] 모델 행 클릭 → 상세 드릴다운 패널 (비용 분해·단가·4xx/5xx 분리) — v1은 테이블 행 클릭 시 사이드 패널을 열어 (1) Cost Breakdown: input/output/cacheRead/cacheWrite 비용을 '토큰수 × $단가/1M' 산식과 함께 표시 + 총비용 + 캐시 절감, (2) Usage: 호출/토큰/지연과 Client Errors(4xx)·Server Errors(5xx) 분리 표시, (3) Pricing: 모델별 1M 토큰당 단가표를 제
- [x] **cloudfront** [S] Enabled/Disabled 배포 수 KPI 카드 — v1은 enabled_count(green)/disabled_count(red)를 별도 StatsCard로 표시. v2는 HIGHLIGHTS[cloudfront]가 없어 status 값 타일로 폴백하는데, CloudFront status(Deployed/InProgress)는 enabled 여부와 무관하므로 비활성 배포가 KPI에 드러나지 않음. `enabled` 컬럼은 이미 동기화됨 — H
- [x] **cloudfront** [S] 상세 패널 Origins 구조화 렌더링 (Origin별 ID + Domain) — v1 상세 드로어는 origins 배열을 origin당 서브카드(ID, Domain 행)로 사람이 읽게 렌더링. v2 inventory-detail.ts structuredList()는 vpc_security_groups/attachments/security_groups 등만 알고 있어 cloudfront origins(객체 배열)는 raw JSON <pre> 코드블록으로 폴백 — Origi
- [x] **cloudfront** [S] 테이블 'Protocol'(ViewerProtocolPolicy) 컬럼 — v1 목록 테이블은 viewer_protocol 컬럼(allow-all / redirect-to-https / https-only)을 노출해 행 단위로 HTTPS 정책을 스캔 가능. v2 columns는 domain_name/status/enabled/price_class뿐. sync_lambda cloudfront SELECT에 `default_cache_behavior ->> 'Viewe
- [ ] **cloudtrail** [S] Event detail drill-down with raw CloudTrail JSON — Clicking an event row in v1 opens a side panel with Event section (event_id, name, source, time, user, access_key_id, read_only badge), Resource section, and a pretty-printed 'Raw Event' JSON block (cloud_trail_event par
- [x] **cloudtrail** [M] Page-content i18n (ko/en/zh) — v1 renders title, subtitle, KPI labels and table headers through t('cloudtrail.*') with Korean/English/Simplified-Chinese support. v2's inventory page hardcodes Korean strings ('전체', '검색…', '로딩 중…', RiskHero verdicts) an
- [x] **cloudtrail** [S] Write Events tab (write-only audit view) — v1 has a dedicated 'Write Events' tab showing only mutating API calls (read_only='false', last 20) — the highest-signal audit view. Absent in v2. Once the events feed exists (previous gap), this is just a read/write filt
- [x] **cloudwatch** [S] Account column in the alarm list (multi-account) — v1's DataTable auto-adds an Account column when isMultiAccount && data[0].account_id, and the detail panel shows an Account row. v2 syncs account_id (sync_lambda.py:251) and shows it in the DetailPanel Identity section, 
- [x] **cloudwatch** [S] INSUFFICIENT_DATA KPI stat — v1 shows a dedicated StatsCard for INSUFFICIENT_DATA count (orange). v2's HIGHLIGHTS for cloudwatch_alarm only defines ALARM (danger), OK (accent), '액션 비활성', and namespace distinct-count — insufficient_data alarms are in
- [x] **cloudwatch** [M] Page-body i18n (ko/en/zh) for titles, KPI labels, column labels — v1's page is fully localized via useLanguage()/t() — t('cloudwatch.title'), t('cloudwatch.okState'), t('cloudwatch.alarmName') etc. in Korean/English/Chinese. v2's i18n core (web/lib/i18n.ts) is explicitly 'shell + navig
- [ ] **cloudwatch** [S] Worst-first default ordering (ALARM rows on top) — v1's alarmList query orders by state_value DESC, state_updated_timestamp DESC, so firing alarms surface at the top of the table with zero interaction. v2's sync SQL orders by name and the page renders in that order; the 
- [x] **compliance** [M] 멀티 어카운트 벤치마크 스코핑 (accountId별 실행/결과) — v1은 useAccountContext의 accountId를 쿼리 파라미터로 넘겨 Powerpipe를 --search-path "public,aws_{id},kubernetes,trivy"로 계정별 스코핑하고 결과 파일도 계정별(_{accountId} suffix)로 분리 저장. v2 POST /api/compliance/run은 benchmark만 받고 worker run_powerpipe
- [ ] **compliance** [M] 컨트롤 상세의 Description 필드 — v1 컨트롤 상세 슬라이드오버는 Status/Reason/Resource에 더해 컨트롤 description(권고 배경 설명)을 표시. v2 worker _walk_controls는 control_id/title/section/status/reason/resource/region/severity만 수집하고 compliance_results 스키마에도 description 컬럼이 없어 Deta
- [x] **compliance** [M] 컴플라이언스 페이지 본문 i18n (ko/en/zh) — v1은 제목/부제/KPI 라벨/버튼/빈 상태 등 페이지 문자열을 t('compliance.*')로 ko/en/zh 3개 언어 지원. v2 페이지는 영어·한국어가 하드코딩으로 혼재('Run Benchmark' vs '실행 중…', '벤치마크 실행 중…')하고 v2 i18n 코어(lib/i18n.ts)는 shell/nav 문자열 MVP 범위라 페이지 본문 미적용. ko/en 키 추가는 소규모이나
- [x] **container-cost** [S] ECS cluster filter with live-recomputed stats/charts/table — Dropdown of cluster names (shown only when >1 cluster, with Clear button); selecting a cluster re-filters the task table and recomputes all KPIs and both charts client-side. v2 ecs_task spec only exposes a launch_type fa
- [x] **container-cost** [M] Per-task Fargate cost estimation (daily/monthly, CPU+Memory split) — v1 computes for every RUNNING Fargate task a dailyCost {cpuCost, memoryCost, totalCost} = (cpu/1024)*vcpuPerHour*24 + (memoryMB/1024)*gbMemPerHour*24, with unit prices overridable via data/config.json fargatePricing; EC2
- [x] **container-cost** [M] `chart` Service Cost Distribution donut (daily $ per ECS service) — Pie chart of estimated daily cost aggregated by ECS service (service: prefix stripped), labels 'name: $x.xxx', with a no-data empty state. v2 has DonutBreakdown ready-made, but no per-service cost data exists; the only E
- [x] **cost** [M] Cost Explorer 가용성 사전 체크 + 전용 Unavailable 상태 (MSP 자동 감지) — v1은 데이터 로드 전에 cost-check API로 가용성을 확인(config.costEnabled로 MSP/Payer 판별 + probe 쿼리, 사유를 'access denied(MSP/SCP)', 'timed out', 'not enabled'로 분류·1시간 캐시)하고, 불가 시 사유 텍스트 + 'Re-check Availability'(캐시 버스트 재확인) 버튼 + 'View Reso
- [x] **cost** [M] cost 페이지 i18n 미적용 (v1: ko/en/zh-Hans, v2: 한국어 하드코딩) — v1 cost 페이지는 useLanguage()의 t('cost.title'|'cost.thisMonth'|'cost.costTrend' 등)로 한/영/중 3개 언어를 지원. v2는 lib/i18n.ts(ko/en)와 LanguageProvider가 존재하고 shell/login/group-overview에는 적용돼 있으나 cost 페이지는 '이번 달 누적', '서비스 필터', '예상 월말 
- [x] **cost** [M] 비용 스냅샷 폴백 + 'Showing cached data' 배너 + Retry Live — v1은 라이브 쿼리 실패/빈 결과 시 디스크 스냅샷(data/cost/, 계정별)을 로드해 페이지를 계속 렌더링하고, 'Showing cached data · Last fetched: <시각>' 오렌지 배너와 'Retry Live' 버튼(재확인 후 라이브 복귀)을 표시. 성공 시 스냅샷을 저장해 다음 장애를 대비. v2는 CE 실패 = 페이지 전체 실패이며 폴백 데이터 소스가 없음. v2에는
- [x] **cost** [S] `chart` 서비스 드릴다운 패널: 월별 추이 라인 차트 + 월별 분해 리스트 + Cost Summary — v1 드릴다운은 해당 서비스의 전체 이력 월별 비용을 (a) 'Monthly Trend' 라인 차트, (b) 월별 금액 행 리스트(Monthly Breakdown), (c) Cost Summary 섹션(서비스명/누적 총액/개월 수)으로 표시. v2 패널은 30일 일별 추이 + usage-type 분해만 있고 월 단위 뷰가 전무 — 장기 추세(수개월 증감)를 드릴다운에서 볼 수 없음. getS
- [ ] **dashboard** [S] 강제 최신화 새로고침 (bustCache → on-demand sync) — v1 헤더 새로고침은 ?bustCache=true로 서버 캐시를 무효화해 Steampipe 라이브 재조회를 강제. v2 새로고침은 Aurora 재조회일 뿐 데이터는 마지막 sync 시점에 고정. v2 대응은 on-demand sync 트리거인데 worker job ALLOWED 셋이 noop/noop-heavy/report/compliance뿐이라 'sync' job type 추가(또는 sy
- [x] **dashboard** [M] 대시보드 본문 i18n (ko/en/zh) — v1 대시보드는 모든 라벨·서브라인·경고 문구·상대시간까지 useLanguage t()로 한국어/영어/중국어(간체) 3개 언어를 지원. v2는 lib/i18n.ts가 shell/nav 문자열 한정(ko/en)이고 대시보드 본문('보안 이슈', '활성 경고', '리소스 현황', 경고 문장 등)은 한국어 하드코딩. 기존 i18n 코어에 페이지 본문 키를 확장하고 zh 사전 추가 필요.
- [x] **dashboard** [S] 데이터 신선도 상태 바 (v1 캐시 워머 바의 v2 대응 = sync 신선도) — v1 하단 상태 바는 마지막 캐시 워밍 시각(상대시간), 소요 시간, 쿼리 수, 갱신 주기, 워밍 횟수, 에러를 표시. v2의 캐시 워머 자체는 Aurora sync 아키텍처로 대체되어 무의미하지만, 사용자 가시 정보인 '데이터가 언제 것인가'는 v2에 부재 — RefreshButton capturedAt은 클라이언트 fetch 시각이지 sync 시각이 아님. inventory_resourc
- [ ] **dashboard** [M] 리소스 타일 서브라인(마이크로스탯) 전면 부재 — v1은 20여 개 StatsCard마다 change 서브라인으로 상태 분해를 표시: EC2 running/stopped, Lambda 런타임 수/장시간 timeout, ECR scan-enabled/immutable, EKS ready 노드/파드/디플로이, CloudFront enabled/HTTP 허용, VPC 서브넷/NAT/TGW, WAF 룰그룹/IP set, EBS 총 GB/미암호화, 
- [x] **dashboard** [S] 비용 타일 지난달 비교 + MoM% 델타 — v1 비용 카드는 일평균 외에 '지난달 $X · ±N% MoM'을 표시(cost.dashboardDetail의 this_month/last_month). v2 비용 타일은 MTD·일평균·월말 예상 청구액만 있고 전월 대비 비교가 없음. v2 /api/cost가 이미 monthly(월별 총액 배열)를 반환하므로 클라이언트 계산만으로 구현 가능 — 신규 API 불필요.
- [ ] **datasources** [S] Curated example-query and NL-prompt chips for all types — v1 Explore offers 4 clickable example chips per datasource type in both modes: raw query examples (EXAMPLE_QUERIES: PromQL/LogQL/TraceQL/SQL/Jaeger/Dynatrace/Datadog) and natural-language examples in AI mode (AI_EXAMPLES
- [ ] **datasources** [S] Dedicated Loki log stream viewer — v1 renders Loki streams in a purpose-built log pane: per-line timestamp, up to 3 colored label badges parsed from stream labels, alternating row shading, 500px scrollable container, and a line count header. v2 renders sh
- [ ] **datasources** [S] Extended time ranges (7d / 30d) — v1's Explore time-range presets go up to 7d and 30d (with coarser steps 3600s/14400s). v2's presets stop at 24h and the query API hard-rejects range.window > 86400s, so multi-day trend exploration is impossible even via 
- [x] **datasources** [L] Jaeger / Dynatrace / Datadog datasource types — v1 supports 7 platforms (prometheus, loki, tempo, clickhouse, jaeger, dynatrace, datadog) with per-type icons, URL placeholders, example queries, AI query-gen prompts, and query execution. v2 supports only prometheus, mi
- [ ] **datasources** [S] Query result metadata bar (rows / execution time / query language / result type) — v1 shows a status strip after every query: 'N rows returned in Xms | PromQL | type: matrix', backed by metadata.executionTimeMs from the API. v2 shows only a truncation warning; there is no row count, latency, or languag
- [x] **dynamodb** [S] KPI: 총 아이템 수 / 총 테이블 크기 타일 없음 — v1은 전체 테이블의 item_count 합계(로케일 포맷)와 table_size_bytes 합계(formatBytes: B/KB/MB/GB)를 StatsCard 2장으로 표시. v2 dynamodb는 HIGHLIGHTS 항목이 없어 제네릭 상태 타일(총 N + top-4 table_status 카운트)로 폴백되며 아이템/용량 합계 KPI가 없다. HIGHLIGHTS에 이미 있는 'sum' 
- [x] **dynamodb** [S] 멀티 어카운트 시 테이블 Account 컬럼(AccountBadge) 자동 추가 없음 — v1 DataTable은 isMultiAccount && data[0].account_id 감지 시 Account 컬럼을 자동 prepend하고 AccountBadge(alias + 컬러 도트)로 렌더해 통합 뷰에서 행 소속 계정을 즉시 식별 가능. v2 dynamodb 테이블 컬럼은 ID/Region + spec 컬럼뿐이고 account_id는 상세 패널 Identity 섹션에서만 보인다(
- [x] **dynamodb** [S] 상세 패널: key_schema/PITR/SSE 구조화 렌더링 없음 (원시 JSON 폴백) — v1 상세 패널은 key_schema를 'HASH → pk' 형태의 KeyType→AttributeName 행으로, point_in_time_recovery_description에서 PITR 상태를, sse_description에서 암호화 Status/SSEType을 추출해 읽기 쉬운 Row로 표시. v2는 inventory-detail.ts의 structuredList에 이 키들의 핸들러가
- [x] **dynamodb** [M] `chart` 차트: 테이블별 아이템 수 Top-10 바 차트 없음 — v1은 item_count>0인 테이블 상위 10개를 BarChartCard('Items per Table', purple)로 표시해 어떤 테이블이 데이터를 갖고 있는지 즉시 파악 가능. v2 inventory 페이지에는 행 단위(per-resource) 값 바 차트가 전혀 없다. v2에 BarDistribution.tsx 컴포넌트는 이미 존재하므로, spec에 barKey/valueKey/
- [x] **dynamodb** [S] 테이블 행의 상태 배지(StatusBadge) 미적용 — v1은 목록 테이블의 table_status를 색상 StatusBadge로 렌더. v2 DataTable의 STATE_KEYS 집합(web/components/ui/DataTable.tsx:14)에 'table_status'가 빠져 있어 목록에서는 일반 텍스트로 표시된다(상세 패널의 inventory-detail.ts STATE_KEYS에는 포함되어 StatePill 적용됨). DataTab
- [x] **ebs** [S] `chart` 볼륨 Encryption 분포 차트 누락 — v1은 Encrypted/Unencrypted 파이차트를 표시. v2는 타입당 도넛 1개(distKey 단일)라 암호화 분포 차트가 없음. spec의 distKey를 distKeys[] 배열로 확장하고 page.tsx에서 도넛을 map 렌더링하면 두 차트 모두 해결(boolean → 'Encrypted'/'Unencrypted' 라벨 매퍼 필요).
- [x] **ebs** [S] `chart` 볼륨 State 분포 차트 누락 — v1은 볼륨 파이차트 3개(Type/State/Encryption)를 나란히 표시하지만 v2 ebs_volume은 distKey=volume_type 도넛 1개만 렌더링. in-use/available/error 상태 분포 시각화가 없음(상태는 SegmentedControl 카운트로만 노출).
- [x] **ebs** [S] 볼륨 throughput 필드 미수집 — v1 상세는 Throughput(MB/s)을 표시(gp3 핵심 스펙). v2 sync_lambda.py의 ebs_volume SELECT에 throughput 컬럼이 아예 없어 상세 패널에도 나올 수 없음. sync SQL에 컬럼 추가 + Storage 섹션 keys에 추가(재수집 필요).
- [ ] **ebs** [M] 상세 패널: 볼륨별 스냅샷 목록 드릴다운 — v1은 볼륨 상세에서 해당 volume_id의 스냅샷 최대 20개(snapshot_id/size/enc/date)를 서브리스트로 표시하고 'No snapshots for this volume' 빈 상태 제공. v2 ebs_volume 상세에는 스냅샷 교차조회가 전혀 없음. ebs_snapshot 데이터가 같은 Aurora 테이블에 있으므로 data->>'volume_id' 필터 BFF 쿼리 
- [ ] **ebs** [M] 상세 패널: 연결된 EC2 인스턴스 교차조회(enrichment) — v1은 볼륨 클릭 시 attachment의 InstanceId마다 aws_ec2_instance를 추가 조회해 인스턴스 Name/type/running-state 배지를 카드로 표시. v2 상세는 attachments idlist(InstanceId/Device/State)만 보여주고 인스턴스 정보 조인이 없음. ec2 rows가 이미 Aurora inventory_resources에 있으므
- [x] **ebs** [S] 스냅샷 테이블 Name 컬럼 — v1 스냅샷 테이블 첫 컬럼은 Name(tags->>'Name'). v2 ebs_snapshot sync SQL에는 name alias가 없고 테이블 컬럼에도 Name이 없어 스냅샷을 이름으로 스캔할 수 없음(상세 헤더만 tags.Name 파생). sync SQL에 (tags->>'Name') AS name 추가 + columns에 name 추가.
- [ ] **ebs** [S] 암호화율(%) KPI + 색상 임계값 — v1은 encrypted_count/total을 %로 계산해 100%=green, 80%+=orange, 미만=red 색상 KPI로 표시(enc·unenc 카운트 서브라벨 포함). v2 HIGHLIGHTS는 '미암호화' 절대 카운트만 있고 비율 KPI가 없음. computeHighlights에 'percent' kind(분자 조건/분모 전체 + 임계값 variant) 추가로 해결.
- [x] **ebs** [M] 페이지 본문 i18n (ko/en/zh-Hans) — v1 EBS 페이지는 제목/서브타이틀/컬럼 라벨을 t()로 3개 언어 지원. v2 i18n.ts는 ko/en 두 언어에 '셸+네비게이션 전용'으로 명시돼 있고 inventory 페이지 본문('전체', '로딩 중…', '데이터 없음')과 HIGHLIGHTS 라벨('사용 중', '미암호화', '총 용량')은 한국어 하드코딩. 사전 확장 + HIGHLIGHTS label의 키化 필요(전 인벤토리 
- [ ] **ec2** [M] 500행 캡 없는 전체 플릿 집계/목록 — v1은 SQL 집계(summary/statusCount/typeDistribution)를 플릿 전체에 대해 실행하고 list도 무제한 로드하므로 KPI·차트·필터 카운트가 항상 전수 기준. v2는 ROW_LIMIT=500으로 fetch를 캡하고 KPI/도넛/facet 카운트를 클라이언트에서 그 샘플로 계산 — 500대 초과 계정에서는 수치가 조용히 부정확해짐(capped 표시는 risk 아키
- [ ] **ec2** [S] 실행 중 총 vCPU KPI 카드 — v1 summary 쿼리는 running 인스턴스의 cpu_options_core_count × threads_per_core 합계를 계산해 'total vCPUs' StatsCard로 표시. v2 HIGHLIGHTS.ec2는 실행 중/중지됨/퍼블릭 IP/타입 종류만 있고 vCPU 합계 카드가 없음. sync_lambda가 vcpus·cores·threads를 행별로 이미 동기화하므로 com
- [x] **ec2** [M] 페이지 본문 i18n (ko/en/zh) — v1 EC2 페이지는 제목/부제/KPI 라벨/컬럼 라벨을 t('ec2.*')로 한국어/영어/중국어(간체) 3개 언어 전환 지원. v2는 lib/i18n.ts가 '셸+내비게이션 전용(MVP scope)'으로 명시돼 있고 zh 미지원(ko/en만), inventory [type] 페이지 본문은 한국어 하드코딩('총', '실행 중', '검색…', '전체 해제', '로딩 중…') + 영문 컬럼 라벨
- [x] **ecr** [S] KPI: Scan-on-Push enabled count — v1 shows a dedicated StatsCard counting repositories with image scanning scanOnPush=true (green tone). v2 has no HIGHLIGHTS.ecr entry, so the page falls back to only the generic total tile — no scan-posture KPI. Requires
- [x] **ecr** [S] Multi-account Account column in table — v1's DataTable auto-adds an Account column when isMultiAccount && data[0].account_id (documented DataTable behavior; detail panel also shows Account at src/app/ecr/page.tsx:84-86). v2 syncs account_id per row and shows i
- [ ] **ecr** [S] Table column: Scan on Push (Yes/No) — v1's list table has a scan_on_push column rendered as green Yes / gray No per repository — key at-a-glance security posture. v2's ecr columns are only URI / Tag mutability / Created; scanning config exists only as raw JS
- [ ] **ecs** [S] `chart` 'Running Tasks per Cluster' 파이 차트 (가중치 차트) — v1은 클러스터별 running_tasks_count를 값으로 하는 PieChartCard를 렌더링(클러스터 이름 × 실행 태스크 수 가중 분포). v2 ecs_cluster의 DonutBreakdown은 distKey='status'의 행 개수 countBy만 지원해 사실상 'ACTIVE 100%' 도넛이 되며, 특정 숫자 컬럼을 카테고리별로 합산하는 가중 도넛은 스펙/페이지 어디에도 없음
- [ ] **ecs** [S] ecs_cluster 맞춤 KPI 카드 (태스크/서비스/컨테이너 인스턴스 합계) — v1 KPI 밴드는 총 클러스터·총 서비스·총 태스크·총 컨테이너 인스턴스 4개 StatsCard를 표시(summary 쿼리의 COUNT/SUM). v2는 HIGHLIGHTS에 ecs_service만 있고 ecs_cluster 항목이 없어 제네릭 상태 타일(ACTIVE: n)로 폴백 — running/pending tasks 합계, active services 합계, registered co
- [ ] **ecs** [S] 정확한 총계 KPI (500행 캡 없는 COUNT) — v1 summary는 COUNT(*)/SUM으로 전체 태스크·서비스 총계를 표시. v2 '총 N' 타일은 fetch된 행 수(ROW_LIMIT=500 캡) 기반이라 ECS 태스크처럼 500개를 넘는 타입에서 총계가 잘림. /api/inventory/summary가 이미 타입별 진짜 count를 제공하므로 총계 타일만 summary byType 값으로 대체하면 됨.
- [x] **ecs** [M] 페이지 본문 i18n (ko/en/zh) — v1 ECS 페이지는 제목·KPI 라벨·컬럼 헤더 전부 t('ecs.*')로 3개 언어(한/영/중) 전환 지원. v2 i18n(web/lib/i18n.ts)은 셸/내비게이션 문자열만 ko/en 지원하며, 인벤토리 페이지 본문은 한국어 하드코딩('전체', '검색…', '로딩 중…', '총 N') + 영문 스펙 라벨 혼용이고 중국어는 전무. 크로스커팅 작업(스펙 라벨 키화 + 페이지 문자열 사전
- [x] **eks-container-cost** [M] `chart` Namespace Cost Distribution 파이 차트 (일간) — 네임스페이스별 일일 비용을 라벨($값 포함) 붙은 파이 차트로 표시, 데이터 없을 때 noData 빈 상태. v2에 DonutBreakdown 컴포넌트가 있어 비용 라우트만 생기면 재사용 가능.
- [x] **eks-container-cost** [S] Node별 비용 컬럼 (Instance Type/Hourly Rate/Daily Cost/Pods) — 노드 테이블에 인스턴스 타입별 시간당 요금과 일일 비용($/day), 노드당 Pod 수를 표시. v2 클러스터 상세 nodes 탭은 이미 노드 목록+노드별 pod 집계를 갖고 있어 정적 가격표 매핑 컬럼만 추가하면 됨.
- [x] **eks-container-cost** [M] Request 기반 비용 추정 폴백 (OpenCost 미설치 시) — v1은 OpenCost 미설정/실패 시 kubernetes_pod 리소스 요청 + kubernetes_node allocatable + EC2 온디맨드 가격표(~40종)로 Pod 비용을 추정한다(CPU비율×0.5 + Mem비율×0.5 × 노드 시간당 × 24). v2는 in-cluster pods/nodes 조회 경로(lib/eks-incluster.ts KIND_PATH)가 이미 있어 데이
- [x] **eks-container-cost** [S] 데이터 소스 배너 (OpenCost 실사용 vs Request 추정 경고) — 상단에 녹색(OpenCost — actual usage: CPU+Mem+Network+Storage+GPU) 또는 노란색(Request 추정 — CPU+Mem만, OpenCost 설치 안내) 배너와 테이블 우측 Source 표기로 데이터 신뢰도를 명시. v2 OpencostPanel은 설치 여부 배지만 있고 비용 계산 방식 구분 표시가 없다.
- [x] **elasticache** [M] Replication Groups 테이블 — v1은 aws_elasticache_replication_group을 조회해 Group ID, Description, Status, Node Type, Multi-AZ(색상 표시), Auto Failover, Cluster Mode, Region 컬럼의 두 번째 DataTable을 표시. v2는 replication group 데이터를 전혀 sync하지 않음 (클러스터 행의 replicati
- [x] **elasticache** [S] 고가치 테이블 컬럼: Repl Group, At-Rest Encryption — v1 클러스터 테이블은 replication_group_id 컬럼과 at_rest_encryption_enabled(Yes/No 녹색/적색) 컬럼을 표시. v2 테이블 컬럼은 engine/version/node type/status/nodes뿐. 두 컬럼 모두 이미 sync되어 있으므로 inventory-types.ts의 columns 배열에 추가만 하면 됨 (미암호화 카운트를 danger 
- [ ] **elasticache** [M] `chart` 상세 패널 CloudWatch 메트릭 스파크라인 (Last 1h) — v1 클러스터 상세 패널은 CPUUtilization, FreeableMemory, CurrConnections, NetworkBytesIn/Out, CacheHitRate의 최근 1시간 시계열을 recharts LineChart 스파크라인 + 최신값(포인트 부족 시 Avg/Max/Min 그리드)으로 표시. v2 DetailPanel은 RDS 전용 라이브 메트릭 섹션만 존재(resourceT
- [x] **elasticache** [S] 엔진/노드 KPI 스탯 카드 (Valkey/Redis/Memcached/총 노드/노드 타입 수) — v1은 7개 StatsCard로 총 클러스터, 총 노드(SUM num_cache_nodes), Valkey/Redis/Memcached 엔진별 개수, Replication Group 수, distinct 노드 타입 수를 표시. v2는 HIGHLIGHTS에 elasticache 항목이 없어 generic state 타일(총 개수 + 상태별 top-4)로 폴백함. countWhere(engine
- [x] **elasticache** [M] 캐시 노드 단위 상세 테이블 (cache_nodes 평탄화) — v1은 클러스터별 cache_nodes JSONB를 파싱해 Node ID, Node Status(배지), AZ(CustomerAvailabilityZone), Endpoint 주소를 노드 단위 행으로 평탄화한 별도 테이블 제공. v2는 sync_lambda.py의 elasticache SELECT에 cache_nodes 컬럼 자체가 빠져 있어 데이터가 Aurora에 없음. sync SELEC
- [x] **iam** [M] Customer-managed IAM policy count KPI (iam_policy data missing entirely) — v1's KPI row shows 'Policies' = COUNT(*) FROM aws_iam_policy WHERE is_aws_managed = false (customer-managed policies). v2 has no iam_policy data at all — grep for iam_policy/is_aws_managed across awsops-v2/web and script
- [x] **iam** [S] Human-readable value formatting for IAM columns (Never / locale dates / session hours) — v1 formats create_date and password_last_used via toLocaleDateString(), renders an explicit 'Never' for a null password_last_used (semantically meaningful: user never used a console password), and converts max_session_du
- [x] **iam** [M] i18n for IAM page body (ko/en/zh) — v1 renders every IAM page string through t() with full Korean/English/Simplified-Chinese support (iam.title, iam.totalUsers, iam.userName, iam.createDate, common.id, ...). v2's i18n (web/lib/i18n.ts) is explicitly 'shell
- [ ] **inventory-home** [M] 계정별(멀티 어카운트) 인벤토리 추이 스코핑 — v1은 useAccountContext의 accountId를 inventory API에 전달해 계정별 스냅샷 디렉토리 + 증분 합산 aggregate를 조회(계정 선택 시 해당 계정 추이만 표시). v2 trend/summary는 account_id='self' 고정 — inventory_resources에는 타깃 계정 데이터가 있지만 inventory_snapshots는 self 카운트만 
- [x] **inventory-home** [S] 대시보드 홈 i18n 미적용 — v1 인벤토리 페이지는 useLanguage t()로 제목/부제/컬럼 라벨을 3개 언어(ko/en/zh)로 제공. v2 GroupOverviewClient는 useI18n을 쓰지만 홈(web/app/page.tsx)은 '대시보드', '리소스 추세 (14d)', '보안 이슈', 활성 경고 문장 등 한국어 하드코딩. i18n 프로바이더가 이미 있으므로 문자열 키 치환만 하면 됨.
- [ ] **inventory-home** [S] 시리즈 표시/숨김 토글 칩 (Core / Other Resources) — v1은 차트 아래에 리소스별 색상 칩을 'Core Resources'(기본 5종 표시)와 'Other Resources' 2그룹으로 나눠 배치, 클릭으로 라인 show/hide. v2에는 인터랙티브 범례/칩 없음. 멀티라인 차트(갭1) 위에 얹는 소규모 작업.
- [ ] **inventory-home** [S] 인벤토리 요약 KPI 바 (Resource Types · Total Count · 7d Net Change) — v1은 추적 중인 리소스 타입 수, 전체 수량 합계, 전 타입 합산 7일 순증감(±색상)을 인라인 KPI 바로 표시. v2 홈/그룹 페이지에 '7d Net Change' 개념이 전무하고 전체 합계(total)도 홈에는 미노출(그룹 페이지 부제목에만). summary+trend 응답으로 즉시 계산 가능.
- [x] **inventory-home** [S] 추이 기간 토글 (30d / 90d) — v1은 차트 상단에서 30d/90d 전환 버튼 제공(90일 이력 보존). v2 홈은 days=14 하드코딩. v2 trend API가 이미 days 파라미터(MAX 90)를 받으므로 UI 토글만 추가하면 됨.
- [ ] **inventory-home** [M] 파생 지표·K8s 카운트의 이력화(트렌드 시리즈) — v1 스냅샷에는 보안 파생 카운트(Public S3 Buckets, Open Security Groups, Unencrypted EBS)와 K8s 카운트(EKS Nodes, K8s Pods, K8s Deployments, ECS Tasks/Services)가 포함되어 추이 차트/델타 테이블에서 시계열로 추적됨. v2 inventory_snapshots는 sync된 resource_type 원
- [ ] **k8s-eks** [S] Cluster/VPC facet filter on EKS overview — v1 has a collapsible filter panel with multi-select cluster chips and VPC chips (each VPC chip shows its cluster count), an active-filter count badge, 'Clear all', a 'filtered/total clusters' counter, and clicking a clus
- [ ] **k8s-eks** [M] Fleet-wide resource drill-down pages from KPI cards — v1 KPI StatsCards link (href) to /k8s/nodes, /k8s/pods, /k8s/deployments, /k8s/services — full-fleet list pages with their own KPI rows and full tables across all clusters (e.g. every pod with node_name in one table; nod
- [ ] **k8s-eks** [S] `chart` Node capacity / system-reserved visualizations — v1 renders 3-segment stacked bars per node — Requested / Available / System-Reserved (capacity minus allocatable) — with a legend and 'avail X | rsv Y' captions (nodes subpage and node detail CPU/Memory cards showing Cap
- [x] **k8s-eks** [S] i18n (ko/en/zh) on EKS pages — v1 k8s pages are fully translated via useLanguage()/t() with Korean, English, and Simplified Chinese (including register-flow status messages and the CLI guide labels). v2 eks pages hardcode Korean strings ('조회 등록', '로드 
- [ ] **lambda** [S] Long Timeout (>300s) KPI with caption — v1's summary query computes long_timeout_functions (timeout::int > 300) and shows it as an orange StatsCard with caption 'Functions with timeout > 300s'. v2 has no numeric-threshold highlight kind. Add a 'countGt' (col >
- [ ] **lambda** [M] `chart` Memory Allocation bar chart — v1 renders a second chart: a BarChartCard histogram of function counts per memory_size bucket (top 10, numerically sorted, purple), alongside the runtime pie. v2's lambda page renders only the single distKey donut (runti
- [x] **lambda** [M] Page-body i18n (ko/en/zh) — v1's lambda page is fully translated via t('lambda.title', 'lambda.functionName', ...) in ko/en/zh-Hans. v2's lib/i18n.ts is explicitly MVP-scoped to shell/nav strings (ko/en only); the shared inventory page hardcodes Ko
- [ ] **lambda** [S] Value formatting: last_modified date, null runtime → 'custom' — v1 renders last_modified via toLocaleDateString() in the table (and toLocaleString() in detail) and displays null runtime as 'custom' (COALESCE in SQL + UI fallback, covering container-image functions). v2 renders the ra
- [ ] **monitoring** [M] `chart` EC2 CPU Top 15 바 차트 — v1 상단에 인스턴스별 평균 CPU 내림차순 Top 15 BarChartCard. v2 EC2 페이지의 분포 차트는 instance_type 도넛뿐 — 활용도 기반 차트 없음. ec2AvgCpu()가 이미 인스턴스별 최신값을 GetMetricData로 받아오므로(현재 평균만 집계) per-instance 값을 노출해 BarDistribution으로 렌더하면 됨.
- [ ] **monitoring** [M] `chart` EC2 Network In/Out 모니터링 (테이블 컬럼 + 시계열 차트 패널) — v1 Network 탭: 인스턴스별 Net In/Out(MB/h) 컬럼을 최대 20개 인스턴스에 대해 자동 조회, Private IP 컬럼, 행별 'Chart' 버튼 → 슬라이드인 패널에 NetworkIn + NetworkOut 24시간 시간별 라인 차트 2개 + Total In/Out(24h) 합계 스탯 타일. v2에는 EC2 네트워크 메트릭이 전혀 없음 (lib/metrics.ts에 RD
- [x] **monitoring** [S] Fleet CPU KPI 타일: High CPU(>80%) 카운트 + Peak CPU 인스턴스 — v1 KPI 행 6개 중 v2가 커버 못 하는 것: High CPU(>80%) 인스턴스 수(0 초과 시 danger), Peak CPU %와 해당 인스턴스 이름, 총 Monitored 리소스 수. v2 EC2 metrics route는 fleet 평균 CPU와 시간당 비용 카드만 반환. ec2AvgCpu의 per-instance 결과에서 count/max를 계산해 카드 2-3개 추가하면 됨.
- [ ] **monitoring** [M] `chart` RDS CPU 14일 일별 추이 차트 (드릴다운) — v1 RDS 탭 행 클릭 → 최근 14일 일별 CPU 라인 차트 + Avg/Max/Min 타일. v2 RDS 상세 패널은 최신 스냅샷 수치 8개만 표시하고 시계열이 없음. metrics route의 ?id= 분기에 daily 시계열(Period 86400, 14일)을 추가하고 AreaTrend로 렌더.
- [ ] **monitoring** [M] `chart` RDS FreeableMemory 24시간 추이 차트 — v1은 RDS 행 클릭(Memory 탭) 시 FreeableMemory(GB) 시간별 24포인트 라인 차트 + Average/Maximum/Minimum 스탯 타일. v2 RdsMetricsSection은 여유 메모리 '최신값 1개'만 표시 — 추이 없음. rdsMetrics()가 이미 GetMetricData를 쓰므로 Values 배열 전체를 반환하도록 확장하면 됨.
- [x] **monitoring** [M] 교차 서비스 모니터링 허브 페이지 (5개 탭) — v1은 /monitoring 단일 페이지에서 EC2 CPU / Network / Memory(K8s+RDS) / EBS IOPS / RDS 탭(리소스 수 배지 포함)으로 fleet 활용도를 한눈에 봄. v2에는 이 교차 서비스 활용도 뷰의 집이 없음 — Monitoring 사이드바 그룹은 cloudwatch_alarm 하나뿐이고 메트릭은 인벤토리 페이지별 KPI 카드로 파편화됨. /eks 같
- [x] **monitoring** [S] 카운터파트 화면 i18n (ko/en/zh) — v1 monitoring은 제목/부제/빈 상태/캐시 라벨을 t()로 3개 언어 지원. v2의 대응 표면인 inventory 페이지와 metric KPI 카드 라벨('평균 CPU', '시간당 비용(USD)', '메트릭 불가' 등)은 한국어 하드코딩. v2에 LanguageProvider/useI18n 인프라가 이미 있으므로(Sidebar에서 사용 중) 키 추가만 필요.
- [x] **msk** [M] `chart` Kafka 버전 분포 파이 차트 — v1은 파이 차트 2개(Cluster State, Kafka Version)를 렌더링. v2는 cluster_type 기준 donut 1개만 있음(distKey='cluster_type'). state는 KPI 타일+SegmentedControl로 부분 대체되지만 Kafka Version 분포 차트는 완전 부재하며, kafka_version 자체가 sync되는 평탄화 컬럼이 아님(provis
- [x] **msk** [S] MSK 전용 KPI 스탯 (브로커 수/암호화/모니터링) — v1 StatsCard 6개: Total Clusters, Active Clusters, Total Brokers(합계), Enhanced Monitoring 수, In-Transit Encrypted 수(전체 대비 색상 경고), Avg Brokers/Cluster. v2는 HIGHLIGHTS.msk가 없어 generic state 타일로 폴백(총 개수 + state별 카운트만). 필요 값들
- [x] **msk** [M] 브로커/컨트롤러 노드 인벤토리 테이블 (kafka list-nodes) — v1은 전체 클러스터의 브로커 노드 테이블(Cluster, Type BROKER/CTRL 뱃지, Broker ID, Instance, VPC IP, ENI, Endpoint, 브로커·컨트롤러 카운트 헤더)과 상세 패널 내 클러스터별 노드 카드(IP/Subnet/ENI/Kafka버전/Endpoint/AddedToClusterTime, KRaft 컨트롤러 목록)를 제공. v2에는 노드 개념 자체
- [x] **msk** [M] `chart` 브로커별 라이브 CloudWatch 메트릭 (CPU/Memory/Network 게이지) — v1은 AWS/Kafka 네임스페이스에서 브로커별 8개 메트릭(CpuUser+CpuSystem 합산, MemoryUsed/Free→%, BytesIn/OutPerSec KB/s, NetworkRx/Tx)을 조회해 노드 테이블에 임계값 색상 진행 바(CPU>80% 빨강 등)로 표시. v2 [type]/metrics 라우트는 ec2/rds만 지원하고 msk 분기 없음. lib/metrics.ts
- [x] **msk** [M] 상세 패널의 구조화된 provisioned 구성 섹션 — v1 상세 패널은 provisioned JSONB를 해체해 Broker Configuration(인스턴스/EBS/AZ/Zones/Public Access + SG·Subnet 칩), Encryption 판정 카드(InCluster 여부에 따라 green/orange 테두리, ClientBroker 모드, KMS 키), Authentication(Unauthenticated/SASL-IAM/S
- [ ] **opensearch** [M] JSONB 구조화 렌더링 (상세 패널 가독성) — v1 상세 패널은 cluster_config를 Dedicated Master/Zone Awareness/Warm/Cold Storage/Multi-AZ Standby 행으로, ebs_options를 Volume Type/Size/IOPS/Throughput으로, VPC를 서브넷/SG 칩 목록으로, 암호화를 KMS Key 포함 색상 패널(green/red 보더)로, Advanced Securi
- [x] **opensearch** [S] OpenSearch 맞춤 KPI 카드 5종 (암호화/네트워크 노출 포함) — v1은 6개 StatsCard 제공: Total Domains(+active), Processing(>0이면 orange), Node-to-Node Encrypted(of N), At-Rest Encrypted(of N), VPC Domains, Public Domains(>0이면 red 'Review access'). v2는 HIGHLIGHTS.opensearch 항목과 stateKey가 
- [x] **opensearch** [S] 고가치 테이블 컬럼: Instance Type/Count, N2N/Rest 암호화, 스토리지 크기 — v1 테이블은 cluster_config JSONB에서 InstanceType과 InstanceCount, ebs_options에서 VolumeSize(GB)를 파생 표시하고, N2N Enc / Rest Enc를 Yes/No 색상 텍스트로, processing을 Active/Processing 상태 배지로 보여준다. v2 컬럼은 engine_version, processing(boolean 
- [ ] **opensearch** [S] 상세 패널 필드 누락 (sync 미수집 컬럼 8종) — v1 detail 쿼리는 service_software_options(업데이트 가용 여부/버전 — 운영상 중요), log_publishing_options(로그 유형별 CloudWatch 로그 그룹), domain_endpoint_options(EnforceHTTPS, TLS Security Policy, Custom Endpoint), auto_tune_options, off_peak_wi
- [ ] **rds** [M] Security Group 인바운드 규칙 체이닝 드릴다운 — v1 상세 패널은 인스턴스의 각 vpc_security_group에 대해 SG 상세를 추가 조회하여 group_id/group_name과 인바운드 규칙(프로토콜, 포트 범위, CIDR+Description, 참조 SG pair)을 파싱해 렌더링하고 'No inbound rules' 빈 상태도 표시. v2는 vpc_security_groups를 idlist(SG ID + Status)로만 표시
- [ ] **rds** [M] `chart` 상세 패널 메트릭 시계열 sparkline 차트 — v1 상세 패널의 'Metrics (Last 1h)' 섹션은 메트릭별(CPUUtilization, FreeableMemory, DatabaseConnections, ReadIOPS, WriteIOPS, FreeStorageSpace) 1시간 시계열 LineChart(recharts, 5분 단위)를 그리고, 포인트가 2개 이하면 Avg/Max/Min 폴백을 표시. v2 RdsMetricsSec
- [x] **rds** [M] 페이지 본문 i18n (ko/en/zh) — v1 rds 페이지는 제목/부제/KPI 라벨/테이블 헤더를 t('rds.*')로 한국어/영어/중국어(간체) 3개 언어 지원. v2 inventory 페이지 본문은 한국어 하드코딩('총', '전체', '로딩 중…', '전체 해제', 메트릭 카드 라벨 등)이고 web/lib/i18n.ts는 ko/en 2개 언어 + 'shell/nav 전용' MVP 스코프로 명시됨 — zh 전체 부재.
- [x] **s3** [S] Region 드롭다운 필터 + Public/Private 액세스 필터 — v1은 리전별 카운트가 붙은 region 드롭다운과 Public(N)/Private(N) 액세스 필터를 제공. v2 s3 spec은 filterKeys/stateKey 미설정이라 텍스트 검색만 가능(facet 인프라는 이미 있음 — spec에 filterKeys: ['region'] 한 줄이면 region 필터 완성; region은 페이지가 prepend하는 컬럼이고 facet label은 
- [x] **s3** [M] S3 페이지 보안 KPI 타일 (Public buckets / Versioning / Logging) — v1 s3 페이지는 4개 KPI 중 3개가 보안 지표: Public buckets(red highlight + 'Requires attention!'/'All private' 문구), Versioning enabled(+'N disabled' 서브텍스트), Logging(+'N without logging'). v2 HIGHLIGHTS.s3는 '리전 수' 1개뿐이라 총계+리전수만 표시됨. p
- [x] **s3** [M] 목록 테이블 고가치 컬럼: versioning/encryption/logging/public 배지 — v1 테이블은 name, region, versioning_enabled(StatusBadge), encryption_enabled(StatusBadge), logging_target(배지), bucket_policy_is_public(PUBLIC 빨간 pill), creation_date 컬럼 제공. v2 s3 테이블은 ID/Region/Created 3개뿐. sync_lambda.py가 
- [x] **s3** [M] 상세 패널 Security/Configuration 섹션 — v1 상세 패널은 Security 섹션(Public Policy + Block Public ACLs/Policy, Ignore Public ACLs, Restrict Public 4종 플래그)과 Configuration 섹션(Versioning, Encryption 구성 여부, Lifecycle Rules 개수)을 행 클릭 시 fresh 쿼리로 표시. v2 s3 상세는 Identity 섹션(
- [x] **security** [L] `chart` CVE Severity Distribution pie chart — v1 renders a PieChartCard of Trivy findings grouped by severity (CRITICAL/HIGH/MEDIUM/LOW/UNKNOWN) with fixed severity colors (red/orange/purple/cyan/gray). v2 has only a 2-slice 'Findings by severity' donut (High vs Med
- [x] **security** [M] Multi-account scoping (account selector + Account column/detail row) — v1 passes accountId: currentAccountId on every fetch, DataTable auto-prepends an Account column (AccountBadge) when isMultiAccount and rows carry account_id, and detail panels show an Account row. v2's /api/security hard
- [ ] **topology** [M] `chart` 5컬럼 인프라 리소스 맵 (Map View) + 교차 하이라이트 — v1의 기본 인프라 뷰: External(IGW/TGW) | VPCs(name/CIDR/id) | Subnets(AZ 배지/CIDR) | Compute(EC2 상태 dot·type, ELB, RDS) | NAT(state) 5컬럼 맵. VPC 클릭→소속 subnet+EC2 하이라이트, EC2 클릭→상위 subnet+VPC 하이라이트, 나머지는 dim 처리, 선택 토글/Clear selecti
- [ ] **topology** [M] `chart` K8s 리소스 맵 (Ingress→Service→Pod→Node) — v1은 K8s Map 탭에서 4컬럼 맵 제공: Ingress(namespace·class·LB hostname) | Services(type·cluster_ip·매칭 pod 수) | Pods(namespace·node) | Nodes(pod 수). ingress rules→service, service selector→pod, pod→node 관계를 클라이언트에서 매핑하고, 검색/클릭 시 4
- [x] **topology** [L] 계정 스코프 토폴로지 (멀티 어카운트) — v1은 useAccountContext의 accountId를 쿼리에 전달해 계정 전환 시 토폴로지가 해당 계정(또는 전체 Aggregator)으로 재렌더링됨. v2 topology는 /api/inventory/* 를 스코프 파라미터 없이 호출하고, inventory 읽기와 topology_nodes/edges 조회가 account_id='self'로 하드코딩되어 계정 전환이 토폴로지에 반영되
- [x] **topology** [S] 다중 매치 검색 하이라이트 (IP·인스턴스 타입 매칭 포함) — v1 검색은 매치된 리소스 전부 + 상위 컨테이너(subnet/VPC, K8s는 연관 svc/node)를 하이라이트하고 나머지를 dim — private_ip/public_ip/instance_type/CIDR 필드까지 매칭. v2 검색은 label/id만 매칭하는 10개 드롭다운에서 단일 노드 focus만 가능(예: 't4g.large'나 특정 IP로 전체 매치 보기 불가). v2 flow
- [x] **vpc** [S] NAT Gateway 목록/상세 (주소, 실패 사유) — v1은 NAT 탭(nat_gateway_id/name/vpc/subnet/state/created)과 상세 패널에 Addresses 섹션(Public IP/Private IP/Allocation ID/ENI per address), failure_code/failure_message, delete_time을 표시. v2에 nat_gateway 타입 없음. sync SELECT + 스펙 항목 
- [ ] **vpc** [M] Transit Gateway 탭 + TGW Attachments + 라우트 테이블 드릴다운 — v1은 TGW 목록(ASN/DNS/state), TGW Attachments 테이블(resource_id/type/state, 행 클릭 상세+options JSON), TGW 상세에서 해당 TGW의 라우트 테이블들과 각 테이블의 라우트(Destination/Type/Target attachment/State)까지 2단계 중첩 조회로 표시. v2에는 tgw 관련 인벤토리 타입/동기화가 전무. 
- [x] **vpc** [L] `chart` VPC Resource Map (인터랙티브 VPC 내부 라우팅 맵) — v1은 VPC 행의 'Resource Map' 버튼으로 전체화면 4컬럼 맵을 염: VPC(CIDR) → AZ별 그룹핑된 서브넷(퍼블릭=초록/프라이빗 구분, 가용 IP 수, 연결 RT 표시) → Route Table(main 배지, 라우트별 dest→target 색상 구분, blackhole 취소선) → 외부 연결(IGW/NAT/TGW/Peering 타입별 색상). 노드 클릭 시 연관 서브넷↔
- [x] **vpc** [M] 페이지 수준 i18n (ko/en/zh) — v1 vpc 페이지는 제목/탭/컬럼 라벨까지 t('vpc.*')로 한국어/영어/중국어(간체) 3개 언어 지원. v2 인벤토리 페이지는 UI 문자열('전체', '로딩 중…', '검색…', '총 N')이 한국어 하드코딩이고 컬럼 라벨은 영어 고정 — v2 i18n 코어(KO/EN)는 셸/내비 크롬에만 적용되며 zh는 부재. 크로스커팅 갭(모든 인벤토리 페이지 공통).
- [x] **waf** [S] Rules 구조화 렌더링 (rule별 Name/Priority/Action 카드 + 'No rules' 빈 상태) — v1 상세 패널은 rules JSONB를 파싱해 규칙별 카드로 표시(규칙 이름 강조, Priority, Action/OverrideAction 요약 80자)하고 규칙이 없으면 'No rules' 문구를 보여줌. v2 DetailPanel은 rules를 원시 JSON 코드 블록으로 덤프(inventory-detail.ts formatDetailValue → kind 'code')하며 빈 배열은
- [x] **waf** [S] 멀티 어카운트 시 테이블 Account 컬럼 — v1 DataTable은 isMultiAccount && data[0].account_id 감지 시 Account 컬럼을 자동 추가(webAclList 쿼리가 account_id 포함). v2 waf 스펙 columns에는 account_id가 없고 v2 DataTable에도 자동 감지 로직 없음 — 여러 계정 스코프에서 어떤 ACL이 어느 계정 것인지 테이블에서 구분 불가(상세 패널 Ide

## P3 (79건)

- [ ] **ai-diagnosis** [S] 경과 시간 타이머 + 완료 후 통계 바 — v1은 생성 중 1초 단위 mm:ss 경과 타이머를 표시하고, 완료 후 '섹션 수 | 경과 시간 | 리포트 ID' 통계 바를 보여준다. v2는 시간 표시가 전혀 없고 ~2분 후 '오래 걸리고 있다' 힌트만 있다.
- [ ] **ai-diagnosis** [S] 생성 중 15섹션 체크리스트 그리드 + 섹션별 서브토픽 표시 — v1은 생성 중 15개 섹션 전체를 완료(초록 체크)/진행(스피너)/대기 상태의 그리드로 보여주고(progress.completedSections), 활성 섹션의 세부 분석 항목(SECTION_SUBTOPICS, 예: 'EC2 인스턴스 비용, Lambda 실행 비용...')과 data-collection 단계의 statusMessage를 표시한다. v2 ProgressPanel은 진행 바 +
- [ ] **ai-diagnosis** [M] 알림 활성화/비활성화 UI 토글 — v1은 UI에서 이메일 알림 전체를 on/off하는 토글(bell 아이콘 버튼 + 패널 내 스위치, 활성 시 이메일 수 배지)을 제공한다. v2는 Terraform diagnosis_notify_enabled(SNS 토픽 존재 여부)로만 기능이 켜지고 UI 토글이 없다 — 관리자가 배포 없이 알림을 일시 중지할 수 없음. DB 플래그 + worker notify.py 체크로 구현 가능하나 v
- [ ] **ai-diagnosis** [S] 인쇄용 리포트 뷰 (브라우저 Print-to-PDF) — v1은 /ai-diagnosis/report?id= 별도 페이지를 새 탭으로 열어 흰 배경 A4 레이아웃, 커버 페이지, 번호 매긴 앵커 TOC, 섹션별 page-break, Print/Close 버튼을 제공한다. v2는 서버 생성 PDF 다운로드는 있으나(download route) 브라우저에서 미리 보고 인쇄하는 HTML 뷰가 없다. PDF 내보내기가 상당 부분 대체하므로 우선순위는 낮음
- [ ] **ai-diagnosis** [S] 초기(빈) 상태의 15섹션 프리뷰 — v1 idle 상태는 진단 범위 설명 문단 + 15개 섹션 아이콘 칩 그리드(cost-overview, security-posture, eks-analysis 등)를 보여줘 무엇이 분석되는지 미리 알린다. v2 빈 상태는 '리포트를 선택하거나 진단 실행을 누르세요' 한 문장뿐이다. sections.py의 섹션 목록을 정적으로 노출하면 됨.
- [ ] **ai-diagnosis** [S] 히스토리 행 내 즉시 DOCX/MD 다운로드 — v1 리포트 이력 테이블은 각 완료 행에 View 외에 DOCX/MD 다운로드 버튼을 인라인 제공해 리포트를 열지 않고도 받을 수 있다. v2 사이드바 목록은 열기(open) 후 본문 상단에서만 MD/DOCX/PDF 다운로드가 가능하다. download route가 이미 있으므로 목록 행에 링크만 추가하면 됨.
- [ ] **bedrock** [S] Models Used KPI 타일 — v1은 기간 내 호출된 모델 수(metrics.length)를 KPI로 표시. v2에는 없음 (models.length로 즉시 구현 가능).
- [x] **bedrock** [S] 모델 테이블 캐시 컬럼 (Cache Hits / Cache Savings) 및 고지연 강조 — v1 모델 테이블에는 Cache Hits(cacheReadTokens)와 모델별 Cache Savings 컬럼이 있고, Avg Latency > 10s는 orange로 강조. v2 테이블(모델/호출/입력/출력/지연/에러/비용)에는 캐시 관련 컬럼이 전혀 없음. 데이터는 v2 API 응답에 이미 존재.
- [ ] **bedrock** [M] `chart` 모델별 시계열 차트 (상세 패널 내 호출 추이·토큰 추이) — v1 상세 패널은 선택 모델의 Invocations Over Time, Token Usage(입력+출력) LineChart 2개를 표시. v2 API(bedrockModelMetrics)는 전체 합산 series만 반환하고 모델별 timeSeries를 버리므로(값 배열만 보존, 타임스탬프는 합산용) 서버 확장 필요 — metrics.ts에서 모델별 {t,value} 보존 + 멀티계정 fan-
- [x] **bedrock** [M] 페이지 본문 i18n (ko/en/zh) — v1 bedrock 페이지는 useLanguage() t()로 제목·KPI 라벨·테이블 헤더·빈 상태 문구를 한국어/영어/중국어 3개 언어로 제공. v2 bedrock 페이지는 한국어 하드코딩이며 v2 i18n(lib/i18n.ts)은 shell/nav 문자열만 커버(ko/en, zh 없음). 페이지 본문까지 i18n 딕셔너리 확장이 필요.
- [ ] **cloudfront** [M] i18n (ko/en/zh) 페이지 문자열 — v1은 useLanguage().t('cloudfront.title') 등으로 한/영/중 3개 언어 지원. v2 inventory 페이지는 '총 N', '검색…', '전체 해제', '로딩 중…' 등 한국어 하드코딩. 페이지 하나가 아니라 v2 전반의 크로스커팅 작업(문자열 카탈로그 + 컨텍스트 도입)이라 이 메뉴 단독으로는 해결 불가.
- [ ] **cloudfront** [S] 테이블 'Name'(tags->>Name) 컬럼 — v1 테이블 2번째 컬럼이 tags->>'Name' 기반 Name(없으면 '--')이라 배포를 사람이 읽는 이름으로 식별 가능. v2는 `name`을 동기화하고 DetailPanel 헤더 타이틀로는 쓰지만 목록 테이블 컬럼에는 없음(resource_id/region+4컬럼). spec.columns에 { key: 'name' } 1줄 추가.
- [ ] **cloudtrail** [S] 'Last Delivery' column in the trails table — v1's trail table shows latest_delivery_time as a 'Last Delivery' column (localized datetime) — an at-a-glance signal that a trail is actually delivering logs. v2's cloudtrail columns are is_logging / multi-region / home_
- [ ] **cloudtrail** [S] Trail detail: CloudWatch Logs delivery + digest delivery + stop_logging_time fields — v1's trail detail shows cloudwatch_logs_role_arn ('CW Role') and latest_cloudwatch_logs_delivery_time ('CW Last Delivery'), and its detail query also selects latest_cloudwatch_logs_delivery_error, latest_digest_delivery_
- [ ] **cloudwatch** [S] `chart` Alarm State Distribution chart (OK/ALARM/INSUFFICIENT_DATA pie) — v1 renders TWO charts: a PieChartCard of alarm states with semantic colors (OK green #00ff88, ALARM red #ef4444, INSUFFICIENT_DATA gray) plus a namespace bar chart. v2 renders only ONE donut, driven by distKey='namespace
- [ ] **compliance** [S] `chart` Alarms by Section 바 차트 — v1은 상태 파이차트 옆에 최상위 섹션별 alarm 건수를 빨간 바 차트(BarChartCard)로 표시. v2는 섹션별 pass-rate Meter 목록만 있고 섹션별 alarm 카운트 시각화가 없음. results에 section/status가 이미 있으므로 클라이언트 롤업만으로 구현 가능하며 v2에 BarDistribution 차트 컴포넌트가 이미 존재.
- [ ] **compliance** [S] 벤치마크 완료 SNS 이메일 알림 — v1은 벤치마크 완료 시 notifyBenchmarkCompleted로 SNS 이메일(벤치마크명, 계정 alias, total/alarm/ok 카운트)을 발행. v2 compliance worker에는 알림 코드가 전혀 없음 — 단 diagnosis worker에는 notify.publish_report(SNS, flag-gated) 패턴이 이미 있어 동일 패턴 재사용으로 구현 용이.
- [x] **container-cost** [M] Container cost KPI tiles — Four StatsCards: total daily Fargate cost, monthly estimate (daily x30), task count with Fargate/EC2 split 'N (F:x / EC2:y)', and Top Cost Service (name + $/day) — all recomputed live when the cluster filter changes. v2 
- [ ] **container-cost** [S] Cost Calculation Basis collapsible transparency panel — Expandable section documenting the Fargate unit-price table (vCPU/Memory/ephemeral storage for ap-northeast-2), the calculation formula, a worked example, and notes (EC2 unsupported, config.json-configurable pricing, 30-
- [ ] **container-cost** [M] `chart` Cost by Service — CPU vs Memory grouped bar chart — Two-series bar chart per service splitting estimated cost into CPU vs Memory components. v2's BarDistribution is single-series (xKey/yKey only), so this needs a small multi-series/stacked extension plus the per-task cost
- [ ] **cost** [S] KPI 타일: Daily Average, Last Month 단독 타일, '증가율 >20% 서비스 N개' 서브 지표 — v1 KPI 6종 중 v2(5종)에 없는 것: (a) Daily Average — 필터된 일별 합계의 평균($) 타일이 완전 부재, (b) Last Month 총액 단독 타일 — v2는 MoM 타일 hint에 '전월 $X'로만 축약, (c) Services 타일의 change 서브텍스트 'N increasing >20%' (전월 대비 20% 초과 증가 서비스 수). v2의 filterDail
- [ ] **cost** [S] 데이터 없음 안내 배너 (Cost Explorer 미활성 가이드) — v1은 로드는 성공했지만 행이 0건일 때 'Cost Explorer may not be enabled — Enable Cost Explorer in the AWS Billing console. Data may take 24h to appear.' 안내 배너를 표시(에러와 구분되는 온보딩 가이드). v2는 빈 데이터 시 '비용 추이 데이터 없음' 카드 문구만 있고 원인/조치(Billing 콘솔
- [ ] **cost** [S] 서비스 테이블 조건부 시각 인코딩 (변화율 임계값 색상 + 점유율 미니 바) — v1 테이블은 Change 컬럼을 임계값 기반 색상으로 인코딩(>20% red, >0 orange, <0 green — 급증 서비스 즉시 식별)하고 Share 컬럼에 미니 진행 바를 렌더링. v2는 두 컬럼 모두 일반 텍스트 문자열('+12.3%', '4.5%')로 변환해 DataTable에 넘겨 정보성 시각 신호가 사라짐. 순수 스타일이 아니라 임계값 의미 전달(정보) 요소. v2 Data
- [x] **dashboard** [S] Cost Explorer 미가용 안내 상태 (N/A + 대체 링크) — v1은 로드 전 action=cost-check로 CE 가용성을 프로브해 미가용 시 타일 값 'N/A' + '비용 데이터 미가용' 안내 문구를 띄우고 링크 타깃을 /cost 대신 /inventory로 스왑. v2는 getMtdCost 실패 시 mtdCost=null → 대시(—)만 표시하고 사유 안내가 없음(트렌드 카드도 '비용 데이터 없음'만). overview 응답에 costAvailab
- [ ] **datasources** [S] AI-generated query explanation banner — v1 shows a purple banner after AI generation ('AI Generated — Generated PromQL query from: "..."' with queryLanguage) so users know the textarea content was drafted and from what prompt. v2's generate silently replaces t
- [ ] **datasources** [S] KPI stat cards on the management view — v1 shows a 4-card StatsCard row above the table: Total Datasources, Prometheus count, Loki count, ClickHouse count (with icons/colors). v2's Datasources tab goes straight to the table with no summary KPIs.
- [ ] **datasources** [S] Manual refresh button on the page header — v1 both datasource pages expose a Header onRefresh action to re-fetch the list/datasources without a full page reload. v2's Datasources tab fetches once on mount; recovering from a stale list requires a browser reload (t
- [ ] **datasources** [M] Per-datasource connection settings (Timeout, Cache TTL, ClickHouse database) — v1's form has a Settings section: request timeout (ms), result cache TTL (s), and a ClickHouse database name field, all persisted per datasource and used by the client. v2's form has no timeout/cache/database settings — 
- [ ] **datasources** [S] Per-row 'Diagnose with AI' action — v1 each datasource row has a stethoscope button that deep-links to the AI assistant with a prefilled message ('{name} ({url}) 연결을 진단해줘'), giving one-click connection troubleshooting. v2 row actions are only Explore/Edit/
- [ ] **datasources** [S] `chart` Tempo trace duration inline bar visualization — v1 renders the durationMs column of Tempo results as a proportional horizontal bar (scaled to the max duration in the result set) next to the numeric value, making slow traces instantly scannable. v2 renders traces as a 
- [ ] **datasources** [L] i18n (ko/en/zh) on all datasource UI strings — v1 wraps every label in t('datasources.*') with three-language support via LanguageContext. v2 hardcodes Korean (with some English labels) across the tab, form, and Explore panel. This is an app-wide v2 decision, not spe
- [ ] **dynamodb** [M] i18n: 페이지 라벨 3개 국어(ko/en/zh) 미지원 — v1 dynamodb 페이지는 제목/부제/모든 KPI·컬럼 라벨을 useLanguage t()로 번역(한국어/영어/중국어 전환). v2 inventory 페이지는 한국어 UI 문자열('전체', '검색…', '로딩 중…', '총 N', 'N개 리소스')과 영어 컬럼 라벨이 하드코딩되어 언어 전환이 불가하다. v2에 nav용 labelKey i18n 인프라는 있으므로 spec 라벨과 페이지 공용
- [x] **dynamodb** [S] `chart` 차트: Table Status 파이 차트 없음 — v1은 table_status별 분포 PieChartCard('Table Status')를 표시. v2의 도넛(DonutBreakdown)은 distKey='billing_mode'라 과금 모드 분포만 보여주고 상태 분포 차트는 없다(상태는 KPI 타일/SegmentedControl 카운트로만 노출). distKey를 table_status로 바꾸면 한 줄이지만 billing 도넛을 잃으므로
- [ ] **ebs** [S] 상세 패널: attachment의 DeleteOnTermination 플래그 — v1은 attachment마다 Delete on Termination Yes/No를 표시(위험 시 orange 강조). v2 inventory-detail.ts의 structuredList('attachments')는 InstanceId/Device/State만 추출하고 DeleteOnTermination을 버림. block_device_mappings가 이미 하는 flag 추출 패턴을 at
- [ ] **ebs** [S] 상세 패널: 암호화/유휴 볼륨 권고 call-out — v1 상세에는 (a) 암호화 여부에 따라 green/red 테두리 verdict 배너(KMS Key 표기 또는 'Consider creating an encrypted copy' 권고), (b) 미연결 볼륨이면 'Idle volume — consider deleting to save costs' 비용 힌트가 있음. v2는 encrypted/kms_key_id를 일반 필드로만 나열. 타입별 a
- [x] **ec2** [S] `chart` Instance Status 바 차트 부재 — v1은 인스턴스 타입 분포 파이 차트와 별도로 instance_state별 카운트 바 차트(BarChartCard 'Instance Status')를 함께 렌더링. v2 ec2는 distKey가 instance_type 하나뿐이라 도넛 1개만 그려지고 상태 분포는 KPI 타일/SegmentedControl 카운트로만 표현됨. v2에 BarDistribution 컴포넌트가 이미 존재하므로 sp
- [ ] **ecr** [S] KPI: Tag Immutability count — v1 shows a StatsCard counting repositories with image_tag_mutability=IMMUTABLE (purple). v2 has no HIGHLIGHTS.ecr, so no immutability KPI tile; the distKey donut shows the MUTABLE/IMMUTABLE distribution but not a complia
- [ ] **ecr** [S] Table column: Encryption type (AES256/KMS) — v1's list table shows encryption_type extracted from encryption_configuration ->> 'encryptionType'. v2's table omits it; encryption_configuration appears only as raw JSONB in the detail Security section. Derive encryptio
- [x] **ecr** [M] i18n for page/KPI/column labels (ko/en/zh) — v1's ECR page localizes everything via t('ecr.title'|'ecr.subtitle'|'ecr.totalRepos'|'ecr.scanOnPush'|'ecr.repoName'|...) with ko/en/zh-Hans support. v2 has lib/i18n.ts + LanguageProvider but the inventory template hardc
- [ ] **ecs** [S] 클러스터 Settings의 가독형 렌더링 (Name/Value 행) — v1 상세 패널은 settings JSONB 배열([{Name,Value}])을 파싱해 항목별 라벨-값 행으로 렌더링('containerInsights: disabled' 식). v2 DetailPanel은 Config 섹션에 settings 키를 배치했지만 formatDetailValue의 structuredList에 'settings' 분기가 없어 raw JSON code 블록으로 폴백.
- [ ] **ecs** [M] 클러스터+서비스 통합 단일 페이지 뷰 — v1은 한 화면에서 요약 KPI, 클러스터 테이블, 서비스 테이블을 동시에 보여줌. v2는 ecs_cluster/ecs_service/ecs_task 3개 사이드바 리프로 분리되고 ECS 서브그룹 레벨 overview 페이지가 없음(컴퓨트 그룹 overview의 타입별 카운트 타일이 부분적으로만 커버). ECS 서브그룹 overview 라우트(/inventory/g/compute 하위) 또는
- [ ] **eks-container-cost** [S] Cost Calculation Basis 접이식 문서 패널 — ▶ 토글로 열리는 계산 근거 문서: Request vs OpenCost 방식 비교표(5개 비용 항목), 두 방식의 수식 블록, 실제 예시 계산(m5.xlarge $0.442/day), EC2 가격 참조 그리드(8종), 주의사항 리스트(Spot/RI 미반영, cross-AZ만 과금 등). 순수 정적 콘텐츠라 이식 용이.
- [ ] **eks-container-cost** [M] `chart` Node Daily Cost + Pod Count 이중축 바 차트 — 노드별 일일 비용(좌축)과 Pod 수(우축)를 한 바 차트에 겹쳐 표시. v2 BarDistribution은 단일축이라 이중축 변형 또는 2개 차트 분리가 필요.
- [ ] **eks-container-cost** [S] i18n (3개 언어 키 기반 문자열) — v1은 useLanguage t('eksContainerCost.title'/'subtitle'/'namespace'/'cpuCost'/'memoryCost'/'noData') 등 키 기반 ko/en/zh 지원. v2 OpencostPanel은 '미설치'/'저장됨'/'관리자 전용' 등 한국어 하드코딩 — v2에 lib/i18n.ts 인프라가 이미 있으므로 키 등록만 하면 됨.
- [ ] **eks-container-cost** [S] 에러/빈 상태 표시 — API 실패 시 빨간 에러 배너, 차트 데이터 없음 시 'noData'/'No node data' 플레이스홀더. v2 OpencostPanel은 상태 조회 실패를 '조회 제한: reason'으로만 격하 표시하며, (아직 없는) 비용 뷰용 에러/빈 상태가 없다. 비용 뷰 구축 시 함께 구현.
- [ ] **elasticache** [S] `chart` Node Type Distribution 바 차트 — v1은 Engine Distribution 파이 차트 + Node Type Distribution 바 차트 2개를 나란히 표시. v2는 distKey='engine' 도넛 1개만 렌더링. BarDistribution.tsx 컴포넌트가 이미 존재하므로 InvType에 보조 분포 키(예: barKey)를 추가하고 [type]/page.tsx에서 렌더링하면 됨.
- [x] **elasticache** [M] i18n (ko/en/zh) 레이블 — v1은 t('elasticache.*') 키로 제목/부제/컬럼 레이블을 3개 언어로 제공 (src/lib/i18n). v2 inventory 페이지는 spec label('ElastiCache', 'Engine' 등) 영문 리터럴 + UI 문자열('전체', '검색…', '로딩 중…') 한국어 하드코딩 혼재. v2에 lib/i18n.ts가 존재하므로 spec label/컬럼 레이블을 i18n 
- [ ] **elasticache** [M] 상세 패널 Security Group 인바운드 규칙 드릴다운 — v1은 클러스터의 SG ID들로 aws_vpc_security_group을 추가 조회해 각 SG의 protocol/port/CIDR/source-SG 인바운드 규칙을 상세 패널에 전개. v2 DetailPanel은 security_groups를 GroupId+GroupName idlist로만 렌더링하고 규칙은 없음. 규칙 데이터는 이미 security_group inventory type(i
- [ ] **iam** [S] Role list Description column — v1's IAM roles table shows the role description inline as a scanning column (with '--' fallback). v2's iam_role spec columns are only create_date/path/role_id/max_session_duration — description exists in the synced data 
- [ ] **inventory-home** [S] 비용 영향 추정 패널 (Cost Impact Estimation) — v1은 30일 수량 변화량 × 타입별 정적 월비용 가중치(RDS $200, NAT $45, EC2 $80 등 14종)로 '+$N/mo est.' 근사 비용 영향을 |영향| 내림차순 리스트로 표시. v2에 상응 기능 없음. 순수 클라이언트 휴리스틱이라 델타 데이터만 있으면 작음.
- [ ] **k8s-eks** [S] Node/pod detail columns: Pod CIDR, Pod IP, Service Account — v1 node detail shows a Pod Info card with Pod CIDR and node created date, and its pods-on-node table includes 'Pod IP' and 'Service Account' columns. v2's node DetailPanel has labels/taints/conditions and a pods table (o
- [ ] **k8s-eks** [S] Page-level no-access banner with error detail + external docs link — v1 shows a prominent page-level banner when zero K8s data is reachable: title/description, the raw Steampipe error string in a mono box, a link to the external docs guide (NEXT_PUBLIC_DOCS_URL/compute/eks-auth, also repe
- [ ] **k8s-eks** [M] Per-ENI live CloudWatch network traffic tiles — v1 node detail fetches live CloudWatch AWS/EC2 metrics per ENI (NetworkIn/NetworkOut/NetworkPacketsIn/NetworkPacketsOut, 5-min period over the last hour) and shows In/Out avg bytes + packet-rate tiles on each ENI card. v
- [ ] **k8s-eks** [M] `chart` Service Resources charts (CPU/Memory per Service) — v1 has a 'Service Resources' chart tab with two top-15 bar charts: 'CPU per Service (millicores)' and 'Memory per Service (MiB)', computed by joining each Service's selector to its running pods' container requests (label
- [ ] **lambda** [S] Avg Memory KPI — v1 shows an 'Avg Memory' StatsCard (mean memory_size across all functions). v2 HIGHLIGHTS.lambda has no average — computeHighlights only supports countWhere/countTruthy/distinct/sum/deprecatedRuntime kinds. Add an 'avg' 
- [ ] **lambda** [S] Code Size table column with human-readable bytes — v1's list table has a Code Size column formatted via formatBytes (B/KB/MB). v2's lambda columns omit code_size entirely from the table (it only appears in the detail panel's Capacity section as a raw byte integer). code_
- [ ] **lambda** [S] Readable Layers list + 'Not in VPC' empty state in detail panel — v1's detail panel parses the layers JSON into 'N layer(s)' plus one row per layer showing the trailing name:version of each ARN, and its Network section shows an explicit 'Not in VPC' message when vpc_id is null. v2's st
- [ ] **monitoring** [M] `chart` EBS 볼륨 실측 Read IOPS 메트릭 + 시간별 추이 차트 — v1 EBS 탭: 볼륨별 실측 Read IOPS 최신값 + 측정 시각 컬럼, 행 클릭 → 24시간 Read IOPS 라인 차트 + Avg/Max/Min 스탯 타일. v2 ebs_volume 인벤토리는 프로비저닝된 iops 설정값만 표시(실측 아님). metrics route에 ebs 분기 추가(VolumeReadOps/VolumeWriteOps GetMetricData)로 구현.
- [ ] **monitoring** [S] `chart` K8s 노드 메모리 Capacity/Allocatable/Reserved 분석 + 노드 메모리 바 차트 — v1: fleet 총 메모리 capacity/allocatable KPI 타일, 노드별 메모리 capacity 바 차트, Memory 탭 테이블의 Reserved %((cap-alloc)/cap) 프로그레스 바, 노드 클릭 시 Capacity/Allocatable/Reserved 3-바 차트 + 수치 타일. v2 /eks 클러스터 Nodes 탭은 allocatable과 pod request 
- [x] **msk** [M] 페이지 본문 i18n (ko/en/zh) — v1 msk 페이지는 t('msk.title'), t('msk.clusterName'), t('common.status') 등으로 한국어/영어/중국어 3개 언어 지원. v2 inventory 페이지 본문은 한국어 하드코딩('전체', '검색…', '로딩 중…', '총 N', 'N개 리소스')이며 v2 i18n(lib/i18n.ts)은 shell/nav 문자열 KO/EN만 커버한다고 명시. ms
- [ ] **opensearch** [M] `chart` Encryption Status 분포 파이 차트 (Full/Partial/No Encryption) — v1은 Engine Version 파이와 별도로 암호화 상태(Full Encryption / Partial / No Encryption) 파이 차트를 렌더링(N2N + at-rest 조합으로 파생). v2는 spec.distKey가 단일 컬럼('engine_version')이라 도넛이 1개뿐이고 암호화 분포 차트가 없다. sync 시 encryption_status 파생 컬럼을 만들어도 페이
- [x] **opensearch** [M] 페이지 본문 i18n (ko/en/zh) — v1 opensearch 페이지는 useLanguage() t()로 제목/부제/컬럼 라벨을 한국어/영어/중국어(간체) 3개 언어로 제공한다. v2 i18n(lib/i18n.ts)은 ko/en 2개 언어에 shell/nav 문자열만 커버하고, 인벤토리 페이지 본문('총', '전체 해제', '검색…', KPI 라벨)은 한국어 하드코딩이며 zh는 아예 없다. 이는 opensearch 전용이 아닌 
- [ ] **rds** [S] `chart` Storage by Instance 바 차트 — v1은 엔진 분포 파이 차트 옆에 인스턴스별 allocated_storage(GB) 상위 10개 BarChartCard를 나란히 표시. v2는 engine donut(distKey)만 있고 인스턴스별 스토리지 바 차트 없음. allocated_storage가 이미 동기화 컬럼이고 BarDistribution 컴포넌트가 존재하므로, InvType spec에 barKey/barValueKey류 
- [ ] **rds** [S] 총 스토리지 합계 KPI — v1 KPI 4개 중 '총 스토리지'(전 인스턴스 allocated_storage 합계 GB) StatsCard가 v2 HIGHLIGHTS.rds(가용/Multi-AZ/퍼블릭 노출/엔진 종류)에 없음. HIGHLIGHTS에 이미 존재하는 { kind: 'sum', col: 'allocated_storage', suffix: ' GB' } 한 줄 추가로 해결.
- [ ] **s3** [M] `chart` 'Security Status' 막대 차트 (Private/Public/Versioned/Logging) — v1은 Private·Public·Versioned·Logging 카운트를 BarChartCard로 표시. v2에는 BarDistribution 컴포넌트가 이미 있으나 s3 행에 versioning/logging/public 컬럼이 sync되지 않아 그릴 수 없음 (s3_public_access의 bucket_policy_is_public donut이 public/private만 부분 커버)
- [ ] **s3** [M] `chart` TreeMap 'Bucket Map by Region' (보안 상태 색상 블록 맵) — v1은 리전별로 버킷을 블록 타일로 그룹핑하고 보안 상태별 색상(Public=red, Versioned=green, Standard=cyan)으로 표시하며, 블록 클릭 시 상세 패널로 드릴다운되고 범례도 제공. v2 s3 페이지는 region DonutBreakdown 1개뿐이며 버킷 단위 시각화가 전혀 없음.
- [ ] **s3** [L] 상세 패널 'IAM Roles with S3 Access' 교차 리소스 드릴다운 — v1은 버킷 상세를 열 때 attached_policy_arns에 S3/AdministratorAccess가 포함된 IAM role 목록(최대 30개)을 함께 조회해 별도 섹션으로 표시. v2 DetailPanel은 이미 로드된 행 데이터만 렌더링하는 순수 spec-driven 구조라 비동기 보조 fetch 확장점이 없고, v2 iam_role sync에도 attached_policy_arn
- [ ] **s3** [M] 상세 패널 Tags 섹션 — v1 상세는 버킷 tags를 key/value 목록으로 표시(빈 경우 'No tags'). v2 s3 행에는 tags가 sync되지 않아 표시 불가 — ListBuckets는 tags를 반환하지 않으므로 per-bucket GetBucketTagging(denial-safe) 수집 필요. DetailPanel의 'tags' 렌더러는 이미 존재하므로 sync만 추가하면 됨.
- [x] **s3** [M] 페이지 본문 i18n (ko/en/zh) — v1 s3 페이지는 t('s3.title'), t('s3.bucketName'), t('common.region') 등 한/영/중(간체) 3개 언어 i18n을 사용. v2 inventory 페이지 본문은 한국어 하드코딩('전체', '검색…', '로딩 중…', 'N개 리소스', HIGHLIGHTS 라벨)이며 v2 i18n core는 shell/nav 한정 ko/en 2개 언어(zh 없음).
- [ ] **security** [S] `chart` 'Security Issues Summary' bar chart across check types — v1 shows a bar chart of per-issue counts (Public Buckets, MFA Issues, Open SGs, Unencrypted, CVE Critical, CVE High), filtering out zero-value bars — a one-glance comparison of which issue class dominates. v2 has no per-
- [ ] **security** [S] Loading skeleton on first fetch — v1 passes data=undefined to DataTable while loading, which renders a loading skeleton, so the user sees an explicit in-progress state per tab. v2 renders zero-valued StatTiles, an empty donut and an empty table before th
- [x] **security** [M] i18n — translated labels (ko/en/zh-Hans) — v1 renders the page title, subtitle, tab labels and KPI labels via useLanguage()/t('security.*') with Korean/English/Chinese support tied to the app-wide LanguageContext. v2 hardcodes English UI strings (and one Korean e
- [ ] **topology** [S] 노드 타입 색상 범례 — v1은 활성 뷰에 맞는 범례 칩(VPC/Subnet/EC2/ELB/RDS/NAT/IGW/TGW 또는 Ingress/Service/Pod/Node)을 색상 스와치로 표시. v2는 kind별 아이콘·색상은 있으나(KIND_LIGHT/DARK, health 색) 이를 설명하는 범례가 없어 TG health 색상 의미 등을 알 수 없음.
- [x] **topology** [S] 토폴로지 페이지 i18n — v1은 title/subtitle/뷰 탭 라벨을 t('topology.*')로 ko/en/zh-Hans 3개 언어 지원. v2 topology 3개 페이지는 한국어 하드코딩 — v2 web/lib/i18n.ts(ko/en)가 존재하나 shell/nav 범위만 커버하고 topology 키 없음. 키 추가 및 적용은 소규모(단 zh 지원은 Lang 타입 확장 필요한 플랫폼 결정).
- [x] **vpc** [S] Internet Gateway 목록/상세 (VPC attachment) — v1은 IGW 탭(igw_id/name/vpc_id/state — attachments를 jsonb로 풀어 VPC별 행 생성)과 상세의 Attachments 섹션(VPC ID/State)을 제공. v2에 internet_gateway 타입/동기화 없음.
- [ ] **vpc** [S] `chart` Subnets per VPC 파이차트 — v1은 VPC별 서브넷 개수 분포를 PieChartCard로 표시(vpc_id 끝 8자리 라벨). v2 vpc 페이지 도넛은 region 분포, subnet 페이지 도넛은 AZ 분포로 이 뷰가 없음. subnet 페이지에 vpc_id 기준 도넛을 추가하거나 distKey 조정으로 해결 가능.
- [ ] **waf** [S] Default Action 사람이 읽을 수 있는 표시 (Allow/Block) — v1은 default_action JSON을 파싱해 액션 키만('Allow'/'Block') 표시. v2는 Security 섹션에서 default_action 객체를 원시 JSON 코드 블록으로 표시.
- [ ] **waf** [M] WAF Rule Groups / IP Sets KPI 카드 — v1은 요약 KPI 3장(Web ACLs, Rule Groups, IP Sets 총 개수)을 표시. v2는 '총 WAF Web ACLs' 타일 1장뿐이며, aws_wafv2_rule_group / aws_wafv2_ip_set 데이터 자체가 sync_lambda.py에 동기화되지 않아 Rule Groups·IP Sets 카운트가 불가능.
- [ ] **waf** [L] i18n (한국어/영어/중국어) 페이지 텍스트 — v1 waf 페이지는 제목/부제/KPI 라벨/컬럼 라벨을 t('waf.*'), t('common.*')로 3개 언어 지원(LanguageContext + localStorage). v2 동적 인벤토리 페이지는 한국어 하드코딩('총', '검색…', '전체', '로딩 중…', '전체 해제')이며 스펙 라벨은 영어 리터럴. waf 전용이 아닌 v2 앱 전체 공통 갭.


> 2026-07-19 배치 2: 계정 스코핑(인벤토리/summary/cost detail), ElastiCache·OpenSearch·MSK 라이브 메트릭, 대시보드 멀티라인 추이+델타 테이블+기간 토글, 컴플라이언스 섹션 드릴다운, IAM admin 게이트, Neptune/OpenSearch Serverless 타입 추가(sync Lambda 재배포는 owner 스크립트 실행 대기).

> 2026-07-20 배치 3: 퀵윈 묶음(파생 컬럼·유휴 EBS KPI·SG 규칙 렌더·보안 점검별 컬럼·MSK bootstrap·datasource URL) + /monitoring 허브(EC2/RDS 라이브 테이블+시계열) + 멀티어카운트(overview/EKS) + VPC 4타입(route_table/nat/igw/tgw — sync apply는 owner 스크립트 대기). 참고: 모니터링 허브 라이브 메트릭은 배포 리전(ap-northeast-2) CloudWatch만 조회 — 타 리전 행은 값 없음.

> 2026-07-20 배치 4 (P1 전량 완료): Prometheus 멀티시리즈+Line/Bar, CloudTrail 최근 이벤트(전체/Write), /topology/infra 전체 그래프(검색 하이라이트·범례; 자동 재구축은 tfvars graph_rebuild_interval_mins=15 apply 대기), 보안 Container CVEs(ECR 스캔 — Trivy의 v2 대응), EKS Cost 탭(OpenCost allocation)+Node ENI 패널, AI 진단 계정 스코프(DB 컬렉터 스코핑+host 전용 소스 정직 강등, worker 이미지 재배포됨). CloudTrail/CVE/MSK bootstrap/그래프 자동화는 owner의 apply-inv-sync-batch3.sh 실행 후 활성.

> 2026-07-20 P2 테마① 배치: S3 보안 SDK 수집(+KPI/컬럼/도넛/facet — sync apply는 apply-inv-sync-p2.sh 대기), elasticache_replication_group·iam_policy 신규 타입, OpenSearch/CloudFront/IAM/DynamoDB 파생 컬럼·구조화 렌더, MSK Broker Config 섹션, spec.barKey Top-N 바 차트(DynamoDB), table_status/last_status StatePill, EBS throughput/스냅샷 Name(sync).

> 2026-07-20 P2 테마② 배치(비용): Bedrock 캐시 요약(적중률 6.7%)·앱 비교·모델 드릴다운(단가/4xx/5xx/비용분해), CE 가용성 프로브+사유별 Unavailable+재확인, 비용 스냅샷 폴백(cached 배너+라이브 재시도), 서비스 드릴다운 월별 추이+Cost Summary, ECS Task Fargate 일/월 비용 컬럼+클러스터 facet+비용 KPI+Top-10 $ 바+클러스터 도넛, EKS CostPanel 소스 배너+Node별 비용+request 추정 폴백.

> 2026-07-20 P2 테마③ (i18n 전면화, ko/en/zh-Hans): i18n 코어에 zh 사전 + 3언어 순환 토글, lib/i18n-terms.ts 용어 사전(~200 용어+매개변수 규칙), 공용 컴포넌트(StatTile/Card/PageHeader/SectionLabel/DataTable/SegmentedControl/DonutBreakdown/RefreshButton/RiskHero/Filters) 레벨 자동 번역 — 전 인벤토리 41메뉴+대시보드+비용+보안+컴플라이언스+모니터링+EKS의 KPI/테이블/헤더/경고문 커버. 미커버 잔여: AI 진단 페이지 산문, datasources UI(영문 위주), 어시스턴트 설명문 일부.
